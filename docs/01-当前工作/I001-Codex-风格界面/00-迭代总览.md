# I001-Codex-风格界面

状态：进行中。

负责人：Codex。

优先级：P0。

更新时间：2026-08-19 00:40（Asia/Shanghai）

## 目标与业务边界

以独立插件交付 Codex 风格的 DSH 侧栏和会话导航体验。安装者不得修改 DSH 源码。第一版将“置顶”定义为工作区置顶；会话轨道定义为当前会话内的轮次跳转。

不在本迭代重写 DSH 聊天渲染、输入框、设置系统或会话持久化。

## 任务清单

- [x] 建立可发布的客户端插件骨架。
- [x] 接管官方 `sidebar` 插槽，并复用 DSH 原生项目树、搜索与会话操作。
- [x] 为工作区置顶状态保留可运行的纯函数自检。
- [x] 完成真实 DSH profile 装配和浏览器验证。
- [x] 实现会话内轮次导航轨道。
- [x] 将技能、插件和连接器接入对应的 DSH 设置分区，并完成直达跳转；归档会话管理交由独立插件提供。
- [x] 恢复 DSH 原生项目与会话浏览区；仅保留项目置顶快捷区。
- [x] 增加专家套件占位；项目和会话操作菜单由 DSH 原生浏览器提供。
- [x] 统一侧栏图标列、项目菜单顺序和深浅色弹窗对比度。
- [x] 补齐包依赖、官方插槽类型合并、footer action 兼容和本地化字典。
- [x] 升级公开 DSH 开发依赖至 `0.1.0-rc.6`，重建锁文件并将 jsdom 集成测试接入 `pnpm test`。
- [x] 收敛开发与测试基建：测试纳入类型检查、补齐 `settings.section` 运行时依赖、消除 sourcemap 告警并动态解析运行时路径。
- [x] 通过公开插槽增加全会话管理面板，不修改 DSH 宿主源码或原生工作区浏览器。
- [x] 用插件工作区树替换原生浏览器，并将会话管理能力嵌入项目和会话菜单。
- [x] 将顶栏搜索升级为全局搜索面板，并简化工作区项目树的展开控件。
- [x] 将专家和技能切换为外部管理插件入口，并恢复无箭头的项目折叠交互。
- [x] 增加设置内“关于”页，显示配套插件状态并提供受限安装入口。
- [x] 新增 `@michengai/dsh-codex-suite` 聚合包，支持一条命令安装 Codex UI 及三项配套插件。
- [x] 修复代码审查 P1 与批次二问题：跨站安装防护、置顶清空竞态、会话头观察器节流与零搬移重排等（详见 [代码审查报告](02-代码审查报告.md)）。

## 阻塞事项

无外部阻塞。

## 验收标准

- 插件安装后无需修改 DSH 源码即可生效。
- 新对话、打开会话、项目重命名、会话重命名、归档和项目排序均调用 DSH 官方客户端服务。
- 置顶状态刷新后保留，失效或损坏的本地数据不会阻塞侧栏。
- 卸载插件后 DSH 默认侧栏恢复。

## 已完成内容

- 创建 bundle 元数据、`cordis.patch.yml`、构建配置和客户端入口。
- `CodexSidebar` 使用 `sidebar` 插槽替换默认侧栏，提供“新建任务、扩展管理、定时任务、个人助理”导航；定时任务和个人助理均为禁用占位。
- 原生 `sidebar.workspaces` 重新接管项目、最近会话、搜索、会话重命名、归档和项目菜单等交互；插件不再维护这些会话列表副本。
- 置顶信息仅保存在浏览器本地，作为原生列表上方的项目快捷区；“管理”弹层可勾选或取消置顶项目。
- 原生会话渲染、工具调用、流式输出、权限和模型选择保持不变；通过稳定的 `data-chat-anchor-key` 为当前会话增加轮次跳转。导航默认仅显示短标记，悬停或键盘聚焦显示提问摘要，当前阅读轮次高亮。
- 会话内容列设为 800px，输入卡片增加最小高度、焦点态和阴影，但仍沿用 DSH 原生 textarea 镜像层，因此输入会继续随内容增长。
- 侧栏导航使用 20px 固定图标列，文字及图标共享垂直基线；菜单和重命名弹窗复用 DSH 语义色令牌，改善暗色模式与禁用保存按钮可读性。
- `package.json` 同时声明 DSH 的 peer 与开发期类型依赖；客户端通过官方 `ui-sidebar`、`ui-settings`、`ui-conversation` 的类型合并注册插槽，不再使用 `as never` 或 `as unknown as`。
- 替换侧栏完整声明并渲染 `sidebar.footer.action`，第三方插件可继续注册其页脚动作；增加官方 client test runtime 的兼容测试用例。
- 文案改由 `michengai.codexUi` 的中英文词典提供；会话锚点和设置跳转的宿主 DOM 兼容代码分别收敛到单一模块，失配时不静默失败。
- 修复主题令牌引用：以 DSH 实际提供的 `label-primary`、`label-secondary`、`label-tertiary` 令牌替换失效的 `text-l*` 令牌；侧栏提升到 14px 基础字号，工作区文件夹对齐导航图标列。
- 移除底部独立“归档会话”入口；导航与工作区图标向左对齐设置图标列；置顶菜单使用“取消置顶”文案。
- 按实测设置图标左边界 16px 再次校正导航与工作区图标左边界。
- 最近列表、子 Agent 过滤和项目/会话弹窗完全复用 DSH 原生实现。
- 移除插件自带技能目录和 Host 接口，技能入口仅跳转 `@michengai/dsh-skills-manager` 的管理页；专家入口跳转 `@michengai/dsh-agency-agents` 的管理页。两个包均声明为可选 peer，缺失时会显示安装提示。
- 侧栏“插件”直达 DSH 原生插件设置；“连接器”按当前会话可用的 MCP 工具目录聚合展示，且不暴露地址、命令或凭证；归档会话设置入口与列表由独立归档插件提供。
- 已验证当前会话读取到 22 项已安装技能，置顶/项目分区、插件跳转和连接器空态均正常。
- 测试纳入 `tsc --noEmit` 类型检查；补齐 `@deepseek-ai/dsh-client-ui-settings-general` 的 peer/dev 声明；`vitest` 运行时路径改用 `createRequire().resolve()` 并移除 node_modules 缺失 `.map` 引发的告警。
- 新增全会话管理面板：过滤子 Agent、空白和已归档会话，提供打开、本地置顶与未读、重命名、归档、派生、ZIP 导出、工作区打开、复制和深链操作；实现只使用公开 `sidebar.footer.action`、`ctx.sessions` 与 `ctx.workspaces`，不依赖会话行 DOM 标记。
- 改为自定义工作区树：以 `sidebar.workspaces` 的更低优先级覆盖原生浏览器，保留项目分组、折叠、搜索、悬停、拖拽排序、会话过滤和原生语义色；项目可通过菜单或拖入“置顶”区域置顶，会话扩展操作直接位于各自三点菜单，不再保留独立会话管理面板。
- 顶栏搜索改为可关闭、可键盘操作的全局搜索面板，基于 DSH 会话与工作区快照搜索普通会话，并提供设置页和新建任务快捷入口；工作区内重复搜索栏及项目展开箭头已移除，主导航与工作区之间增加语义分隔线。
- 项目文件夹点击可切换展开状态但不显示额外箭头；会话左缩进收紧，工作区置顶星标不再显示。
- 轮次导航默认仅显示短横线，悬停或键盘聚焦后放大为 32px 高摘要卡片；侧栏品牌标识放大至 24px。
- 折叠侧栏收紧为 24px；轮次导航位于其右侧 16px 且在视口左侧中部垂直居中，默认显示 7px 低对比度微刻度，当前轮次加长到 12px，悬停或键盘聚焦后展开 32px 高摘要卡片；会话状态标记不再占用标题缩进，标题与项目标题对齐；扩展二级菜单左移 4px。
- 移除工作区行的运行/等待状态点；本地未读是蓝点的唯一来源，打开会话会清除该标记。
- 运行中的会话在行右侧显示旋转状态圈，并继续与本地未读蓝点分离。
- 侧栏根容器在浅色主题使用 `#eef7f5`，暗色主题使用 `#1c1f1e`；暗色主、次、弱文字色固定为 `#d1d5d3`、`#9ca39f`、`#747b77`，避免继承宿主的纯白文字。
- 侧栏与工作区树统一采用 Codex 风格系统无衬线字体栈；主导航为 14px/500，二级导航和会话标题为 14px/400，分组和时间为 12px，图标使用独立的低亮度层级。
- 设置面板调整为宽 1000、高 800；窄视口仍限制为距四边各 24px，避免溢出。
- 专家与技能入口可通过设置分区检测对应可选插件是否已加载；DSH 未提供安全的公开安装 API，不能在浏览器确认后自动拉取和安装最新包。
- 置顶会话状态改为会话标题后、时间前的低亮度描边图钉，不再在标题前显示星标；会话菜单及实现已移除“在新窗口中打开”，为后续桌面端嵌入保留单窗口模型。
- 工作区树已移除“最近”分区，未归属项目的会话不再单独渲染。
- 当前会话所属项目的文件夹图标使用业务色高亮；项目菜单已通过 DSH 公开 `workspaces.delete` 接入删除项目注册的确认弹窗，不删除文件夹或会话记录。
- 项目展开时显示打开文件夹图标，折叠时显示关闭文件夹图标，便于区分层级状态。
- 会话三点菜单新增永久删除操作，复用独立归档管理插件的远程删除服务；确认删除后同步清理本地会话置顶和未读标记，未安装归档插件时明确提示。
- 删除会话移至菜单底部的独立分隔栏；移除 ZIP 导出入口、下载实现和文件名工具。
- 未读会话按 Codex 样式在行最右侧显示蓝点，替代时间文本且不会改变会话标题的左侧对齐；悬停或打开操作菜单时让位给三点菜单。
- 会话行悬停显示置顶与归档快捷图标；右键会话拦截浏览器默认菜单并复用完整三点菜单。
- 悬停置顶快捷操作使用与已置顶状态一致的图钉；右键菜单的三点定位锚点保持不可见。
- 项目行右键复用可见三点的完整项目菜单，不改变该三点入口的显示与点击行为。
- 设置最后新增“关于”页：介绍 Codex UI 的侧栏、搜索、工作区、会话与轮次导航能力，展示专家、技能、归档管理插件的红绿安装状态；缺失时只允许通过当前 DSH CLI 从 npm 安装固定的受信任包，并由 DSH 同步 web profile bundle 列表后提示重启。
- 新增纯配置型 `@michengai/dsh-codex-suite` 包：以普通 npm 依赖安装 Codex UI、专家、技能和归档管理包，并汇总四者的 DSH patch；独立安装模式继续保留。
- 侧栏顶栏改为 LOGO、收缩、搜索从左到右排列；折叠后展开按钮改挂到右侧主面板覆盖层。
- 任务/频道/定时页签改为按插槽占用显示：只有 IM 插件注册 sidebar.channels 才显示「频道」，只有定时插件注册 sidebar.schedule 才显示「定时」；两者都未安装时不渲染页签栏。
- 收缩侧栏改为立刻进入纯图标窄轨：隐藏标题、文字菜单和工作区树，仅保留顶部展开按钮和底部设置图标。
- 依赖安装 POST 端点增加 `Sec-Fetch-Site`/`Origin` 与 Host 的同源校验（`crossSiteRequest`），阻断恶意网页跨站表单触发安装；非浏览器客户端不受影响。
- 工作区列表未就绪（`items` 为空）时跳过置顶清理，修复挂载竞态把置顶清空并写回 localStorage 的数据丢失。
- 会话顶栏重排改为 CSS `display:contents` + `order`，页签只打标记、标题按钮以兄弟节点插入，不再物理搬移宿主 React 节点（消除宿主重渲染白屏风险）；观察器按 rAF 节流并加 `applying` 重入保护，气泡文本改用 `textContent`。
- 用户气泡被编辑为富文本或空文本时回退原气泡，不再展示过时的替换卡片；轮次导航恢复纵向滚动并截断轮目摘要至 72 字符；频道轮询加在途保护防止慢响应覆盖新数据；顶栏三点菜单支持再次点击关闭；会话悬停卡片标题不再出现双文件夹图标。
- 会话头/气泡装饰新增 jsdom 行为测试（含宿主重建标题后的孤儿清理），并新增 `@types/jsdom` 开发依赖。
- 定时会话标题启发式收紧为必须带 ` - ` 分隔符，避免手动改名为时间开头的普通会话被误藏。
- 依赖安装错误不再回传本地绝对路径；npm latest 查询加 5 分钟缓存，安装加进程级互斥。
- 频道/定时共用 `useSessionFlags`/`useHoverTip`/`useSessionDialogs`/`SessionModals`；清理 CompanionTree、findWorkspaceForSession、pendingPinRef、未用图标导入与 13 个死 i18n 键；英文词典去掉中文残留并加 CJK/死键断言；`pnpm-workspace.yaml` 占位符改为 `esbuild: true`；`npm test` 在 bundle 断言前先跑 `tsdown`。

## 待处理事项

归档会话设置入口与列表已从本插件移除，交由独立归档插件提供。会话三点菜单仍保留 DSH 原生归档操作；会话置顶和未读是当前浏览器的本地增强，不写入 DSH 会话数据。关于页当前只管理默认 `web` profile，其他 profile 仍需通过 DSH CLI 管理插件。聚合包尚未发布至 npm；发布前只可用本地路径安装，且不得与单独安装的子包并存。

## 关联提交

`52079e3 feat: refine workspace and conversation navigation`

## 阅读顺序

1. [实施计划](01-实施计划.md)
2. [技术架构基线](../../00-交接入口/05-技术架构基线.md)
3. `src/client/CodexSidebar.tsx`


