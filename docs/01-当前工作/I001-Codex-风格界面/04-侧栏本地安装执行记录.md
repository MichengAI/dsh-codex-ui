# 侧栏本地安装执行记录

更新时间：2026-09-08 12:16（Asia/Shanghai）

以下保留 2026-09-08 各轮实现、测试和安装时的原始记录，按原记录倒序排列。历史“待验收”已由用户本轮确认完成，截图路径仅说明当时证据，不作为当前交付物或有效链接。当前结论见[迭代总览](00-迭代总览.md)。

- 空聊天居中补充（随本轮提交）：浏览器新增置顶、分组内部、无自定义分组的普通项目列表三场景，后者复现上 5px/下 17px，原因是直接项目列表误用 `.dcu-wb-collections` 的 12px 组间距。新增 `:has(>.dcu-wb-project){gap:0}`，只排除直接项目列表，真实分组间距保持 12px。修复后三场景均为上下 5px，合计 45 组浏览器检查、构建及 32 个断言通过，截图 `tmp/workspace-chat-center-20260908.png` 已查看。
- 最新本地包：`C:\Users\YUJIYU\.dsh\local-packages\codex-ui-chat-center-20260908-1122\michengai-dsh-codex-ui-0.2.111.tgz` 已安装 web profile，安装前 manifest/lock 同目录备份；相同 Desktop store 重装 `codex-ui-empty-center-20260908-1118` 的 tgz 可回退本次修复。实际 Desktop 效果待用户确认。

- 提交后补充（本轮提交 `fix: align sidebar empty states`）：前轮提交为 `df0def9`，未推送。本轮按真实上下胶囊边界重新平衡空状态，有下一分组或未分组时内部 14px/2px 加外部 12px，形成上下各 14px；末尾无下一组时保留内部 8px/8px。两种空状态统一为 13px/18px/三级灰色；空聊天上下 padding 改 1px/5px，补齐行高减少的 2px，保持原项目高度。尚未推送或发布。
- 验证与安装：浏览器脚本新增空状态真实几何、排版一致性和折叠检查，深浅主题与三种宽度共 42 组合通过，空状态上下距离均为 14px。截图 `tmp/workspace-empty-center-20260908.png` 已检查；构建与 32 个断言通过。本地包 `C:\Users\YUJIYU\.dsh\local-packages\codex-ui-empty-center-20260908-1118\michengai-dsh-codex-ui-0.2.111.tgz` 已安装，保留同目录安装前 manifest/lock；同一 Desktop store 重装 `codex-ui-empty-copy-20260908-1112` 的 tgz 可回退，实际 Desktop 效果待用户确认。

- 空状态补充：分组直属 empty 的 padding 改为 `8px 8px 8px 26px`，上下对称，外部组间距仍 12px；`workspace.noChat` 中文改为“暂无聊天”。浏览器检查上下各 8px、收起高度 0，截图 `tmp/workspace-empty-spacing-20260908.png` 已查看；构建及 32 个断言通过。本地包 `C:\Users\YUJIYU\.dsh\local-packages\codex-ui-empty-copy-20260908-1112\michengai-dsh-codex-ui-0.2.111.tgz` 已安装 web profile，同目录有安装前 manifest/lock；相同 Desktop store 重装 `codex-ui-empty-align-20260908-1108` 的 tgz 可回退，实际 Desktop 效果待用户确认。

- 空状态对齐：仅将 `.dcu-wb-collection-body>.dcu-wb-empty` 左留白从 8px 调为 26px，匹配标题的外层 4px、按钮 4px、箭头 14px 和间隙 4px，不改变项目、会话胶囊或纵向间距。Chromium 源码样式测试 DOM 在 240/275/520px 下文字左边界差均为 0px，截图 `tmp/workspace-empty-alignment-20260908.png` 已检查；构建和 32 个断言通过。
- 空状态本地包：`C:\Users\YUJIYU\.dsh\local-packages\codex-ui-empty-align-20260908-1108\michengai-dsh-codex-ui-0.2.111.tgz` 已安装到 web profile，同目录保留安装前 manifest/lock；可用相同 Desktop store 重装前一版 `codex-ui-drop-style-20260908-1103` 的 tgz 回退。本轮实际 Desktop 效果待用户确认。

- 蓝线样式对齐：只读检索本机 `OpenAI.Codex_26.901.6511.0` 的 `app/resources/app.asar`，定位侧栏 `UX({indented})` 组件，原生非缩进规格为直线 left-2/right-2/h-0.5/rounded-full、圆环 left-1/size-2/border-2。采用相同的圆环尺寸、线宽、起点和右边界，圆环内部透明以兼容侧栏背景，线段从圆环右侧描边接入，消除旧渐变断口；明确 border-box 避免伪元素圆环撑大。不修改垂直几何及横向胶囊布局。
- 样式验收及本地包：横向尺寸回归先失败后通过，113 项测试、32 个断言及构建通过；36 组浏览器组合检查线宽、圆环尺寸、居中及滚动，几何误差 0px；截图 `tmp/workspace-drop-style-20260908.png` 已检查。安装包为 `C:\Users\YUJIYU\.dsh\local-packages\codex-ui-drop-style-20260908-1103\michengai-dsh-codex-ui-0.2.111.tgz`，同目录保留安装前 manifest/lock；通过相同 Desktop store 重装前一版 `codex-ui-drop-center-20260908-1057` 的 tgz 可回退样式调整。Desktop 实际视觉仍待用户确认。

- 当前本地安装：`C:\Users\YUJIYU\.dsh\local-packages\codex-ui-drop-center-20260908-1057\michengai-dsh-codex-ui-0.2.111.tgz` 已替换 web profile 的 UI 依赖，复用 Desktop store；前后端构建哈希一致，其他依赖声明与 bundle 顺序不变。同目录保留安装前 manifest/lock；可通过同一 store 重装上一版 `codex-ui-session-gap-20260908-1048` 的 tgz 回退本次定位改动。

- 插入线统一定位：新增 `workspace-drop-indicator.ts`，复用原有排序落点 class，在拖拽期间挂载树内独立绘制层并禁用旧伪元素。按上下可见行的实际边界取中点，跳过收起内容；空置顶和末尾槽按槽边界处理。逐帧跟随滚动、宽度与折叠动画，结束时移除绘制层和监听，不改排序持久化及胶囊布局。
- 蓝线验收：7 项几何与清理测试，总计 113 项 Vitest、类型检查、构建、32 个断言通过。`scripts/verify-workspace-drop-layout.mjs` 输出真实源码样式与定位函数组成的浏览器测试脚本；agent-browser 在 Chromium 中完成深浅主题、240/275/520px、六类落点 36 组组合及滚动测量，误差均为 0px，并检查展开项目收起后重新选取边界。截图证据为 `tmp/workspace-drop-layout-20260908.png` 和 `tmp/workspace-drop-layout-light-20260908.png`；这是测试 DOM 的浏览器验收，不等同于 Desktop 原生拖拽验收。

- 会话列表上下间距：顶部沿用 4px，新增列表内部 `::after` 4px 底部留白；会话内部沿用 2px，不改胶囊背景、横向布局或空项目提示。置顶、分组内与未分组项目共用该规则，留白随折叠内容裁剪。新增样式回归先失败后通过，106 项测试、32 个断言、类型检查与构建通过；真实宿主视觉效果待用户验收。
- 会话间距本地包：`C:\Users\YUJIYU\.dsh\local-packages\codex-ui-session-gap-20260908-1048\michengai-dsh-codex-ui-0.2.111.tgz`；同目录保存安装前 manifest 与锁文件。通过相同 Desktop store 重装前一版 `codex-ui-group-drop-20260908-1045` 的 tgz 可恢复本次间距改动。

- 分组顶部插入线修复：项目分区原本只对 `.dcu-wb-drop` 解除裁剪，漏掉 `.dcu-wb-group-order-drop`，导致首组 `top:-8px` 的蓝条被裁掉。仅对展开且存在分组落点的分区增加 `overflow:visible`；收起状态和无落点状态不变。新增 4 项回归测试，其中 2 项在修复前以 `clip` 不等于 `visible` 失败，修复后 106 项测试、32 个断言脚本、类型检查及构建通过。DOM 计算样式已验，未宣称真实宿主拖拽截图验收通过。
- 最新本地包：`C:\Users\YUJIYU\.dsh\local-packages\codex-ui-group-drop-20260908-1045\michengai-dsh-codex-ui-0.2.111.tgz` 已安装至下述 web profile，客户端与 Host 文件哈希一致，仅 UI 依赖声明变化。该目录另存本次安装前 manifest 与锁文件；需退回本次修复前版本时，可用相同 Desktop store 重装 `codex-ui-sidebar-20260908-1035` 中的旧 tgz。待用户重启 Desktop 验证真实侧栏。

- 本地安装：已将当前构建通过本地 tgz 安装至 `C:\Users\YUJIYU\.dsh\profiles\web`，复用 Desktop 的 `D:\Tools\DSH Codex Desktop\plugins\store`。客户端与 Host 文件 SHA-256 均与工作区构建一致，仅 Codex UI 依赖声明变化，bundle 顺序未变。待重启 Desktop 后进行实际侧栏验收。
- 安装恢复：原 `package.json` 与锁文件备份保留在 `C:\Users\YUJIYU\.dsh\local-packages\codex-ui-sidebar-20260908-1035`；原 Codex UI 版本为 `0.2.111`，需要恢复时通过同一 Desktop store 重新安装该 registry 版本。备份与 tgz 为恢复和后续安装所需，予以保留。

- 三层胶囊间距修正：分组内首个项目增加 4px 顶部留白，与项目到首条会话的既有 4px 间距一致。间距放在折叠容器内部的成员节点，避免容器收起后残留空隙；横向尺寸不变。源码样式预览同步更新。

- 状态：进行中；负责人：Codex；前序业务 REST 改动已提交 `16369d3`。侧栏视觉、拖拽指示线及空状态调整随本轮 `fix: refine sidebar hierarchy and drag indicators` 提交，尚未推送、发布；本地截图证据保留，不纳入版本控制。
- 已实现：分组常驻 14px 加粗箭头、14px/600 标题、32px 行高、55% hover 混色底带和 12px 组间距；展开与收起保持同一轻底色，状态由箭头表示。项目随展开状态切换等尺寸的打开和闭合文件夹；项目保留 8px、会话保留 28px 原有内边距，保持胶囊布局。未分组复用层级结构并降低文字权重。
- 已验证：102 项 Vitest、32 个断言脚本、类型检查、构建；本轮预览 DOM 检查确认新旧两版项目和会话内边距一致、分组折叠前后底色相同、折叠与会话选择有效。上一轮已检查项目键盘折叠、未读清除与主题切换。
- 待验收：浏览器深浅主题、240px 窄侧栏、触摸目标和实际宿主拖拽。内置浏览器拒绝本地 HTML URL，本轮不宣称完成截图或真实宿主验收。
- 对照预览由 `scripts/preview-workspace-hierarchy.mjs <输出HTML路径>` 生成，使用 HEAD 与工作区的实际 CSS、示例 DOM 数据及 Lucide 图标；不接入宿主业务，图标与宿主原生组件可能有细节差异。
- 恢复：本轮仅修改展示样式和对应测试，无数据迁移；可单独撤销本轮视觉差异，前序提交不受影响。
