# 侧栏动画发布验收

更新时间：2026-09-08 15:51（Asia/Shanghai）

状态：已完成。负责人：Codex。用户授权的提交、推送、发布均已完成。

## 交付范围

- Codex UI 升至 `0.2.113`，修复侧栏内容挤压、宽度监听空转，以及 `dsh-better-sidebar` 高优先级 CSS 覆盖网格动画的问题。
- 固定动画期间的内容宽度，合并两侧面板过渡，保留拖拽即时响应与减少动态效果行为。
- Suite Installer 按既有流程在 UI 发布后自动升版并锁定最新成员。

## 验证

- 修复前真实 desktop 计算样式仅有 `padding-right`，收起第一帧直接到 56px；修复后正式本地包重载验证展开/收起各 57 个中间帧，最大采样间隔 10/11ms。
- 已补充组合 CSS、动画中间帧、拖拽、减少动态效果与快速反向切换回归；具体安装与恢复见[本地记录](04-侧栏本地安装执行记录.md)。
- 发布版本本地 `pnpm test` 通过：125 项测试、33 个断言脚本、45+12 组布局及组合动画检查；试打包 18 个预期文件，无临时验证脚本。Node.js 22/24 CI 与全部发布流程通过。

## 提交与发布

- 修复与版本提交 `c495de9`，自动 Installer 提交 `0b4cfbd`，均已推送并同步本地。
- [Node.js 22/24 CI](https://github.com/MichengAI/dsh-codex-ui/actions/runs/34200895890)、[UI npm](https://github.com/MichengAI/dsh-codex-ui/actions/runs/34201066379)、[Release 说明](https://github.com/MichengAI/dsh-codex-ui/actions/runs/34201066360)、[Installer 发布](https://github.com/MichengAI/dsh-codex-ui/actions/runs/34201204125) 全部成功。
- npm latest 为 UI `0.2.113` / Installer `0.1.31`；安装器仅将 UI 更新为 `0.2.113`，其他七个成员版本不变。
- [UI Release](https://github.com/MichengAI/dsh-codex-ui/releases/tag/v0.2.113) 和 [Installer Release](https://github.com/MichengAI/dsh-codex-ui/releases/tag/suite-installer-v0.1.31) 均已逐字核对，与对应双语 CHANGELOG 一致，中文在前。

## 恢复

- 使用 `0.2.112` 公开版可回退发布变化；本机保留逐轮 tgz 和安装前 manifest/lock，无数据迁移。
