# 新建会话布局重做

本地阶段提交：本轮新建页、任务卡片、工具菜单、配置文件入口、官方标题及拖拽宽度恢复纳入 `feat: refine new conversation layout and settings presentation`；仅本地提交，不推送或发布。下方未提交表述为各次安装时的历史状态，当前以此处为准。最终全量验证：157 用例、33 契约检查及全部布局检查通过。


更新时间：2026-09-09 10:05（Asia/Shanghai）

状态：进行中；重做已接入正式 UI 并安装，未提交、推送或发布；正式宿主观感和完整首次发送待复验。

## 撤回与范围

上一轮选择了 Codex 的内联输入首页分支，与用户后来明确的图 1 不符。用户要求撤回并重做。已替换其整组居中及 42svh 标题锚点规则，不保留其有效设计结论；之前已提交的输入框外观、设置、费用统计不回滚。

新目标：中部独立 logo、原主文案及预览标记，下面四类任务卡；底部独立靠左的工作区/预设容器，再接原输入框及统计信息。保留 32px 空间和现有 active 会话布局，不新增假“本地/main”控件，不修改宿主和社区插件。

## 实现

- new-conversation-style.ts：新建页 flex 分区，外层上限 768px，输入卡片 736px；工具容器两侧再缩进 12px，左对齐，底部与输入框叠合 10px。品牌与标题分行，四列任务卡在窄容器降为两列，内容过高可滚动。
- NewConversationSuggestions.tsx：向宿主标题 stack 贡献自有 portal；不搬动原节点、不重建编辑器。四类入口展开各自两项任务建议，选择任务才预填，不自动发送。分类与任务均有中英文，DSH 能力范围内提供建议，不声称完整移植 Codex 的插件模板和所有二级任务。
- new-conversation-draft.ts：通过宿主 input.setDraft 填入空白草稿；未选择工作区时引导选择，已有内容/附件/提及或正在处理时提示并保留原状态。
- React 与 react-dom 使用宿主同一实例。已从实际 DSH Web 启动器 index-Df-65__b.js 的静态模块表核对 react-dom；声明 peer、构建外置并更新模块契约，避免打包第二套渲染器。

## 验证

- 类型检查通过；30 个 Vitest 文件 / 155 用例通过，后续国际化键显式化后新增 2 用例复验通过。
- 构建及 33 个断言脚本通过；57 组原布局、4 条设置退出、材质回退与输入框检查通过。
- 新建页 Chromium 使用原宿主发布 CSS、结构夹具和正式 SuggestionCards 静态渲染，覆盖深浅主题 × 4 个视口。明确检查输入框位于底部区域、工具条左对齐、标题与输入不重叠、长工作区不溢出、统计滚动可达及 active 样式隔离。深色截图已目视确认 logo/标题/四卡和底部工具容器分离。
- 新组件行为测试覆盖展开分类后预填、草稿/附件/提及保护、处理状态阻断、首页退出/重入和卸载清理。
- 以上截图是组件夹具，不能替代正式宿主完整观感和首次发送验收；本轮未重启用户原生窗口，也未重复访问此前被内置浏览器阻止的候选宿主端口。

## 安装与恢复

使用 Desktop 自带 Node/pnpm、原 store，以 offline/ignore-scripts 安装至 web profile。安装包与安装前清单、锁文件备份位于 `C:/Users/YUJIYU/.dsh/local-packages/codex-ui-home-bottom-20260909-0820`，安装 client.js 与工作区 SHA-256 一致。

若需恢复重做之前的正确基线，使用同一 Desktop 工具链重装 `codex-ui-composer-natural-20260909-0730/michengai-dsh-codex-ui-1.0.0.tgz`，不要恢复已否定的 `codex-ui-new-conversation-20260909-0758` 布局包，也不要只覆盖清单。

## 引导区位置修正

用户确认只下移引导区、保持底部输入区。撤销顶部最多 140px 的固定定位，改为在输入区上方剩余空间中垂直居中，四卡展开时按整组高度重新居中。底部工具条、输入框与统计尺寸规则不变。

加入 2272×1400 大窗口及引导区中心对齐断言，深浅主题 × 5 视口通过；类型检查、构建、33 断言通过，本轮纯 CSS 调整未重复全量 Vitest。大窗口夹具截图已目视确认，正式宿主仍待用户重载复验。

最新安装包及安装前备份为 `C:/Users/YUJIYU/.dsh/local-packages/codex-ui-home-center-20260909-0823`，安装 client.js 与构建哈希一致。仅回退本次位置修正可重装 `codex-ui-home-bottom-20260909-0820`；回到重做之前正确基线仍使用上节的 composer-natural 包。未提交、推送、发布。

## 卡片展开位置稳定

用户反馈展开子任务导致整组上跳。根因是条件挂载增加居中区域高度。改为所有分类面板在同一 grid 单元内共同参与尺寸计算，按实际最长内容预留空间；只有选中面板可见，其他面板 aria-hidden、按钮 disabled。提示文案同样预留，避免草稿保护提示再次撑高。无需固定像素高度，可随语言和窄窗口换行。

两项新建页组件行为测试、类型检查和构建通过；深浅主题 × 5 视口增加四类展开、提示切换和收起前后几何完全相同检查，通过。隐藏任务不进入焦点顺序；未重复全量测试，正式宿主观感待重载复验。

安装包与安装前备份：`C:/Users/YUJIYU/.dsh/local-packages/codex-ui-home-stable-20260909-0839`，client.js 哈希与构建一致；回退本项使用 `codex-ui-tool-menus-20260909-0834` 包。未提交、推送或发布。

## 输入内容时隐藏任务建议

按用户补充，有文字草稿时隐藏任务卡片、子任务与提示，清空后重新显示；logo、主标题和底部工具保留。通过 useSyncExternalStore 订阅当前会话原 input.state，切换会话时解除旧订阅，支持输入、粘贴、历史召回、程序预填；使用 visibility 保留占位并禁用所有隐藏按钮，避免跳动或隐形焦点。

3 项新建页测试、类型检查、构建和深浅主题 × 5 视口布局检查通过。覆盖草稿显隐、按钮禁用、清空恢复、切换输入订阅与释放，以及隐藏卡片前后几何不变。未重复全量测试；正式宿主仍待重载复验。

安装包与安装前备份：`C:/Users/YUJIYU/.dsh/local-packages/codex-ui-home-draft-20260909-0850`，client.js 哈希一致；回退本项使用 `codex-ui-home-stable-20260909-0839`。未提交、推送或发布。

## 恢复原品牌区

按用户要求删除 headline、fishHitbox、headlineText、previewBadge 的局部样式覆盖，包括窄屏标题字号覆盖；恢复宿主原横排 LOGO、标题和预览标记。沿用原 34px LOGO、26px/500 标题、32px 行高和 10px 列间距，以及原 LOGO 悬停行为。任务卡片、草稿显隐与底部输入区布局保留。

类型检查、构建和深浅主题 × 5 视口布局检查通过；未重复全量测试，正式宿主观感待重载复验。

安装包与安装前清单、锁文件备份：`C:/Users/YUJIYU/.dsh/local-packages/codex-ui-home-brand-20260909-0859`。已安装到正式 web profile，client.js SHA-256 与构建一致。仅恢复本次品牌调整可用同一 Desktop Node/pnpm/store 重装 `codex-ui-home-draft-20260909-0850/michengai-dsh-codex-ui-1.0.0.tgz`。未提交、推送或发布。

## 恢复输入框继承拖拽宽度

2026-09-09：新建页此前将 composer-card-max-width 固定为736px、外层限制768px，覆盖了宿主 dsh-chat-user-width → chat-content-width → composer-card-max-width 的宽度链。现删除固定输入宽度，外层复用宿主宽度公式，仅中部引导区域保留768px上限。输入框与工具条跟随宿主保存的拖拽宽度，不改持久化或已有会话。

新增560/960px用户宽度检查，先确认固定736导致失败，再修复通过。深浅主题×5视口、底部统计、草稿显隐与active隔离等检查及构建、类型检查通过。已安装codex-ui-composer-width-20260909并核对client.js哈希；安装前清单备份同目录，回退本项可重装codex-ui-official-headings-20260909。未提交、推送或发布；正式宿主拖拽后新建的交互待用户复验。
