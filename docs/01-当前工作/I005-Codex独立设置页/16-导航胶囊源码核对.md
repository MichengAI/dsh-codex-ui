# 设置导航胶囊源码核对

更新时间：2026-09-09 00:10（Asia/Shanghai）

用户明确要求按 Codex 源代码核对，不根据截图估算。本次读取本机已安装的 Codex Desktop 26.901.6511.0 的 app.asar 内前端打包代码，并非公开 CLI 仓库，也未修改 Codex 安装文件。

## 来源锚点

安装资源：`C:/Program Files/WindowsApps/OpenAI.Codex_26.901.6511.0_x64__2p2nqsd0c76g0/app/resources/app.asar`。

- `webview/assets/settings-page-ed0dbe72a147.js`：设置导航导入 `app-primary` 的 `dh`（导航项）和 `fh`（分组）；导航使用 `sidebar-navigation`、`px-row-x`，展开分组间距 `gap-4`。
- `webview/assets/app-primary-428a0a65766f.js`：实际导出的 `dh=xJ`、`fh=SJ`。导航项固定 token 行高、w-full、gap-2，左右使用 padding-row-cell-x；分组子列表使用 gap-px；键盘焦点圈内收 2px。
- `webview/assets/app-initial-5b0a474bff5e.css`：sidebar-navigation 的 height-token-nav-row=30px、padding-row-cell-x=8px、padding-row-x=8px。sidebar-item 使用 radius-lg；桌面 radius-lg-base=.625rem，支持 corner-shape 时乘 1.25 并使用 superellipse(1.5)，因此实际半径为 12.5px；不支持时回退 10px。不要把声明的 radius-token-row=10px 误认为最终计算圆角。
- 同一 CSS：spacing-token-sidebar 使用 clamp，最小 240px、默认偏好 275px、最大 520px，并为正文保留 320px。设置页面的 legacy layout 使用 w-token-sidebar。

## 修改与验证

- 导航项从 32px 无间距改为 30px + 1px，图文间距从 9px 改为 8px，圆角和内收焦点圈遵循上述规则；返回应用按钮共用行高与圆角。
- 导航左右各 8px，边界线改为内阴影以免占用内容宽度。使用现有 dcu-sidebar-expanded-width 偏好，默认导航 275px，胶囊宽 259px；240px 导航时宽 224px。宽度不是与视口无关的固定数值。
- 分组间距 16px、组标题到列表间距 4px。保留现有小于等于 600px 的横向导航和 40px 触摸高度，横向布局不使用纵向 1px margin。
- 生产组件 Chromium 实测：深浅主题高度 30px、行距 1px、宽度 259px、图文间距及左右内边距 8px；支持 superellipse 的浏览器计算半径 12.5px。
- 1280px 桌面及 800/600/390px 视口检查无页面横向溢出；原 4 条退出及原生材质/实色回退检查通过；类型检查、构建与 git diff --check 通过。
- [生产设置导航：选中 Agent 预设并悬停定时任务](15-导航胶囊源码对齐.png)。该图用于检查修复效果，尺寸事实源是上述打包代码。

状态：本地代码与构建已更新，未安装到运行中的 DSH，未提交、推送或发布。本次不实施费用插件内嵌。
