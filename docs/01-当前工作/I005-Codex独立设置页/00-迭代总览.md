# I005 Codex 独立设置页

状态：进行中

负责人：Codex

优先级：P1

更新时间：2026-09-09 18:56（Asia/Shanghai）

## 当前阶段与验收

- 本轮验收：UI 1.1.1 与九成员集合包均重跑完整测试通过（31 文件 / 164 用例、33 契约脚本及全部布局）；真实宿主及安装全流程仍待验收。关联提交 `37b224c`、`c5087fb`。
- 当前发布、提交与安装基线统一见[当前状态](../../00-交接入口/02-当前状态.md)，未关闭事项见[待办](../../00-交接入口/03-待办与阻塞.md)。提交 `5733d8c` 及后续评审增量的实现与验证证据见[修复与安装记录](24-桌面透明设置侧栏重叠.md)。
- 历史发布：UI 1.1.0（`faa621c`）及安装器 1.0.1（`8a5bf69`）已提交、推送、发布；远端测试与两套发布流水线通过，官方 npm 和双语 Release 核对一致。
- 历史验收：UI 1.1.0（`faa621c`）完整 pnpm test 通过：31 文件、164 用例、33 契约脚本及全部布局/生产费用协议检查；不作为后续增量的全量测试结论。
- 独立预览页面和 design-qa 已按用户要求删除，test:host 访问真实 DSH。当前宿主要求认证，本次设置退出实机检查未通过验收。
- 完整 Desktop、真实费用配置、首次引导和发送继续待验收，见[待办](../../00-交接入口/03-待办与阻塞.md)。
- 以下为历史阶段记录，不代表当前发布状态；当前结果见[当前状态](../../00-交接入口/02-当前状态.md)。

## 历史阶段记录

## 目标与边界

最新工具菜单增量：已修复底部项目菜单定位并统一工具菜单样式，增加项目搜索，保留原宿主功能；已安装、未提交。全量测试通过，见[适配记录](22-输入区工具菜单适配.md)。

最新布局增量：已按用户要求撤回上一轮错误首页分支，改为中部引导卡片（已修正为剩余空间垂直居中，并预留展开空间避免上跳，有文字草稿时隐藏任务建议，品牌区恢复宿主原横排 LOGO 与标题）、底部独立工具条和输入框，保留聊天统计留白。已正式接入并安装，未提交、推送或发布；沿用已完成的输入框样式。验证、源码差异与恢复入口见[新建会话布局](21-新建会话布局.md)。

最新增量：用户随后授权输入框实施顺序第 1、2、3 项，已安装，并纳入本地提交 `fix: align composer visuals and natural height with Codex`；未推送或发布。下方输入框小节的“未提交”为当时交付状态，当前以此处为准。沿用最近全量 153 用例、33 断言及全部布局检查通过的记录，本次只提交不重复运行。

阶段交付：本轮将下方历史“未提交”修改汇总为本地提交 `feat: integrate usage settings and refine desktop visuals`；Desktop 配套提交 `f7692fc`。未推送或发布，原生 Desktop 尚未安装，UI 已安装。此前测试结果沿用：UI 全量 152 用例及后增加载生命周期 1 用例单独通过，33 断言、57 布局和退出检查通过；提交前 Desktop 构建及 3 项主题测试通过。输入框设计仍停留在只读评估。历史阶段的安装和未提交状态不代表当前交付状态。

用户选定 Codex 截图（图 4），要求还原独立设置页的样式。采用 240px 设置侧栏、768px 正文、24px 页面标题、深灰分组卡片与轻边框。内容使用 DSH 已注册能力，不复制 Codex 独有的设置或权限语义。

## 任务与验收

- 已完成：设置专用导航、分类搜索、常规分组、深浅主题与窄屏布局。
- 已完成：公共插槽接入；保留原始设置条目、close 回调、首次使用引导和连接重试能力；未知插件仍可访问。
- 已完成：返回应用和焦点恢复，不卸载底层会话；内层 dialog/alertdialog 的 Escape 不退出设置。
- 已完成：删除旧的全局 dialog 尺寸覆盖，避免误伤第三方确认框。
- 已完成：评审修复后 147 项测试、33 个断言脚本及 57 组 Chromium 布局检查；真实注入生命周期、配置文件失败路径、引导与焦点均补齐行为测试，见[评审修复与验收](09-评审修复与验收.md)。
- 已完成：修复新旧设置壳重复声明子插槽导致独立页未生效；停用原壳，保留官方 Host 引导 schema 和公开配置文件操作。详见[宿主加载修复](08-宿主加载修复.md)。
- 待处理：在实际 DSH 宿主安装后的插件详情、权限与配置保存、首次使用引导及连接重试实机验收。负责人 Codex/用户；下一步在实际宿主重载后验证，见[项目待办](../../00-交接入口/03-待办与阻塞.md)。

## 验收边界

浏览器预览直接渲染生产设置壳与分组组件，条目使用示例数据，未连接配置接口；模型预览的保存仅改变预览状态。第三方插件内容在预览中明确显示说明，不能把预览验证等同于宿主全链路验收。当前使用应用内独立视图，不新增 URL 路由或浏览器历史记录。

## 恢复

恢复原设置壳时，需同时撤销 `ui-settings-general` 停用 patch、额外 Host schema 调用及新设置注册，避免重复声明。未修改业务配置和会话数据；无需数据恢复。安装恢复入口见下方记录及修复记录。阶段改动见本节所在本地提交；未推送、未发布。

## 关联提交

前阶段 f1bc3ac；本轮随 fix: address standalone settings review regressions 本地提交，未推送。

## 阅读顺序

1. [最新评审修复与验收](09-评审修复与验收.md)
2. [整体对照](03-整体对照.png)、[局部对照](04-局部对照.png)
3. `src/client/CodexSettingsPage.tsx`：设置壳与通用分组。
4. `src/client/settings-page-registration.ts`：公共插槽投影与替换。
5. `scripts/verify-settings-exit.mjs`：实际 DSH 退出设置检查；设置 `DCU_DSH_URL` 后运行 `pnpm test:host`，不使用预览夹具。

## 本地安装记录

- 最新安装：0.2.114 退出闪烁修复包已安装（codex-ui-exit-0.2.114）；前一轮评审修复包已安装，并实测全局搜索设置根入口通过，见[最新安装与恢复](09-评审修复与验收.md)。

- 历史安装：2026-09-08 21:12 修复包，含留白和连接器滚动修复，含搜索聚焦和重复标题修复，已消除插槽包装额外4px偏移，含悬停及进出动画，见[宿主加载修复](08-宿主加载修复.md)。后续已解锁并完成深色模式下 13 个分类默认页面的实机视觉审阅，未覆盖全部弹窗与配置保存；以下 20:15 记录为首次失败安装的历史证据。

- 2026-09-08 20:15：用户明确要求安装到 DSH 供测试。已用 Desktop 自带 Node.js、pnpm 11.24.0、原 store 安装至 `C:\Users\YUJIYU\.dsh\profiles\web`，采用 `--offline --ignore-scripts`。
- 测试包：`C:\Users\YUJIYU\.dsh\local-packages\codex-ui-settings-20260908-2010\michengai-dsh-codex-ui-0.2.113.tgz`；同目录保留 `package.before.json` 和 `pnpm-lock.before.yaml`。
- 仅 UI 依赖声明改为本地包，其他依赖声明和 bundle 顺序一致。安装后的 client.js、index.mjs SHA-256 均与工作区构建一致。
- 安装成功；pnpm peers check 报告 profile 中缺少宿主依赖。Desktop runtime 独立提供 Cordis 4.0.2、settings-general 和 connection 0.1.2-rc.1，未据此扩充 profile 或重配环境。实际加载与第三方插件组合仍待重启后测试。
- 未重启正在运行的 Desktop，用户完全退出并重新打开后测试。安装前 UI 声明为 `^0.2.113`；恢复时使用同一 Desktop pnpm/store 重装该 registry 版本，备份清单和锁文件用于核对。不要仅覆盖清单冒充依赖恢复。

## 阶段性提交（0.2.114，未发布）

- 用户要求阶段性整理，随后明确只提交、不推送；不创建远端标签、不发布 npm；本机安装后续已更新，见最新验收记录。
- 插件市场实际 section ID 为 `market`，侧边卡片为 `better-sidebar`；分别使用 Store 和 PanelRight，保留 16px、1.6px 描边规格，未知插件继续使用默认图标。
- 本机深色模式 1362×901 下已检查全部 13 个分类的默认页面；外壳背景基本一致，插件内页仍有独立视觉。社区插件内部样式保持不动。
- 新图标已安装进 DSH，本轮截图确认市场与侧边卡片图标不同。
- 浅色主题、全部展开内容、配置保存、首次使用引导与连接重试实机验证继续由项目待办承接。
- 本次验证：131 项 Vitest 测试、33 个断言脚本、57 组 Chromium 布局检查及生产组件图标预览通过；0.2.114 构建、类型检查和 Git 空白检查通过。旧文字偏移断言已随当前网格对齐规则更新。

## 1.0.0 发布

UI 与 Installer 1.0.0 均已提交、推送、发布；之前的仅本地提交限制针对历史阶段。发布结果见[1.0.0 发布验收](10-1.0.0发布验收.md)，实机待办保留。

## 原生侧栏材质（未发布）

已完成 Desktop Mica 与 UI 透明层、首页和设置配色统一、浏览器实色回退，以及深浅主题原生预览验证；本轮未提交、未安装。完整宿主验收待处理，见[原生材质验收](11-原生材质验收.md)。

## 导航胶囊源码对齐（未发布）

按本机 Codex Desktop 打包代码对齐 30px 行高、1px 行距、8px 内边距、圆角及侧栏宽度规则；生产组件深浅主题与布局检查通过，未安装。见[源码核对记录](16-导航胶囊源码核对.md)。

## 费用原面板 iframe 实验

独立承载页已运行原费用插件面板，六分区及关闭、主题验证通过；原插件文件未改，使用隔离空数据，尚未正式接入。见[承载验证与后续边界](18-费用面板独立承载验证.md)。

## 使用统计正式接入（已安装，未发布）

费用插件原面板已通过 iframe 接入正式设置，并安装进本机 DSH；真实数据加载截图与恢复方法见[正式接入与安装](20-使用统计正式接入与安装.md)。费用试验阶段已完成，后续以正式接入记录为准。

## 设置默认入口与宿主文案补译（已安装，未提交）

- 普通设置入口每次打开常规；快捷入口仍选择指定分区，退出时不提前切换内容，避免退出动画闪动。
- 原宿主补译已于 2026-09-11 按用户要求移除：对话显示、权限名称和六条内置指令说明交给官方 i18n；旧版允许显示英文。插件自身词典保留，详见 I006 样式修复记录。
- 全量验证：28 文件 / 151 用例、33 断言脚本、57 布局检查及 4 条设置退出检查、材质回退检查通过。新增回归先确认普通入口仍停留模型页的失败，再修复。正式宿主本次新文案尚未逐项实机验收。
- 安装包与清单备份：`C:/Users/YUJIYU/.dsh/local-packages/codex-ui-i18n-20260909-0049`。正式 profile 五项 UI/frame 构建资源 SHA-256 与工作区一致；未替换 Desktop 程序。
- 恢复到修改前：使用 Desktop 自带 Node/pnpm 和原 store，重新安装 `codex-ui-usage-20260909-0035/michengai-dsh-codex-ui-1.0.0.tgz`，不要仅覆盖依赖清单。UI 版本仍为 1.0.0，未提交、推送或发布。

## 计费入口加载闪烁修复（已安装，未提交）

- 根因：快捷入口先打开常规再经下一帧选择目标分区；iframe 默认浅色文档在主题同步前已可见，加载提示移除还会改变 iframe 高度。
- 自有设置壳通过可取消导航事件一次提交目标分区和打开状态，保留旧宿主的 DOM 导航回退。iframe 就绪前隐藏并退出焦点顺序，固定承载区域内显示加载状态，就绪后原位显示原面板。
- 全量 28 文件 / 152 用例、33 断言脚本、57 布局及 4 条退出检查、材质回退检查通过；后增 iframe 加载生命周期 1 用例单独通过，类型检查通过。快捷入口回归先确认出现 general 的失败再修复。真实 DSH 闪烁观感尚待用户复验。
- 正式包与备份位于 `C:/Users/YUJIYU/.dsh/local-packages/codex-ui-usage-loading-20260909-0054`；安装后五项构建产物 SHA-256 一致。恢复时用 Desktop 自带 Node/pnpm 与原 store 重装 `codex-ui-i18n-20260909-0049/michengai-dsh-codex-ui-1.0.0.tgz`。未提交、推送、发布或修改原费用插件。

## 输入框视觉第 1、2 项（已安装，未提交）

- 参考本机 Codex Desktop 26.901.6511.0 的 app-primary ComposerLayout 与 app-initial tokens；去掉整框强调色焦点圈，浅色采用输入框专用柔和投影，深色采用细内侧亮边与宿主 layer-2 实色背景。
- 圆角基础 20px，支持 superellipse(1.5) 时为 25px；输入区域左右 12px、工具栏左右与底部 8px、主体间距 4px。发送按钮从 34px 和上移 2px 改为 28px 原位居中、中性前景背景，保留禁用状态与按钮键盘焦点。输入最小高度 44px，卡片最小高度 96px 和原宽度规则保留。
- 新增 verify-composer-style.mjs，使用宿主发布 CSS 和已核对 DOM 结构进行无服务 Chromium 验证，不连接真实会话。覆盖深浅主题、390/800/1280px、整框聚焦前后稳定、多行增长、按钮尺寸、键盘焦点和横向溢出；加入 test:layout。
- 全量 29 文件 / 153 用例、33 断言、原 57 布局、4 退出和材质检查及新输入框检查通过；正式宿主观感与完整 IME/附件/发送交互尚未实机复验。
- 安装包与备份：`C:/Users/YUJIYU/.dsh/local-packages/codex-ui-composer-20260909-0720`。正式 client.js SHA-256 一致。恢复时使用同一 Desktop Node/pnpm/store 重装 `codex-ui-usage-loading-20260909-0054/michengai-dsh-codex-ui-1.0.0.tgz`。未提交、推送或发布。

## 输入框自然高度第 3 项（已安装，未提交）

- 用户确认后移除整卡 `min-height:96px`，保留编辑区 `min-height:44px`；其余第 1、2 项视觉和原宽度规则不变。空态由内边距、编辑区和工具栏自然组成 92px，多行内容继续撑高。
- 更新旧的整卡最小高度契约；Chromium 检查空态恰为 92px、多行高度严格增长。全量 29 文件 / 153 用例、33 断言、全部布局/退出/材质及输入框检查通过。
- 正式安装包及清单备份：`C:/Users/YUJIYU/.dsh/local-packages/codex-ui-composer-natural-20260909-0730`，client.js SHA-256 一致。恢复到第 1、2 项版本可用同一 Desktop Node/pnpm/store 重装 `codex-ui-composer-20260909-0720/michengai-dsh-codex-ui-1.0.0.tgz`。
- 本轮未提交、推送或发布；完整原生宿主的附件、IME 和发送交互仍待复验。

## 配置文件入口降级为常规页高级项

按用户确认，将自有 open-document 从全局 settings.action 移至常规页专用 settings.general.footer，放在编辑器分组之后。独立高级卡片包含配置文件标题、说明及次级打开按钮；补齐中英文、打开中文案和禁用防重复请求。其他插件的公共 settings.action 贡献保持原协议。本机访问限制、元数据失败提示及原宿主打开接口保留，不修改社区插件。

24 项设置组件与真实注册生命周期测试、33 契约断言、构建及类型检查通过；生产配置行 Chromium 深浅主题 × 390/1280px 检查通过。已安装 codex-ui-settings-document-20260909，client.js 哈希一致；清单备份同目录。恢复可用 Desktop pnpm/store 重装 codex-ui-mode-height-20260909。未提交、推送或发布，正式宿主外部编辑器打开仍待复验。

## 模型标题及正文顶部留白

更新时间：2026-09-09 10:05（Asia/Shanghai）

模型页原生 h2 保持宿主的 14px 小标题，和常规页24px页面标题不一致；配置入口移走后空 heading 自动隐藏，原先由按钮额外撑开的空白也消失。为正文标记当前 section，只对模型根标题复用常规字号、行高、字重及字距，不新增重复标题、不影响弹窗标题。正文顶部显式设为48px，900px及以下视口32px，避免依赖操作按钮占位。窄屏两页标题同步22px。

深浅主题 × 390/800/1280px 标题样式和顶部位置检查通过，构建及类型检查通过。补齐原设置验证夹具对新增 general.footer 插槽的分支处理后，4项设置退出和材质回退检查通过。已安装 codex-ui-settings-heading-20260909，client.js哈希一致，备份同目录；回退本项可重装 codex-ui-settings-document-20260909。未提交、推送或发布，正式宿主重载观感待复验。

## 官方设置页标题统一

2026-09-09：按用户确认，将 plugins、agent-presets 根标题加入 models 的局部标题规则，统一24px/600/32px，窄屏22px；三页说明14px/22px，沿用宿主12px布局间距。仅覆盖已知官方section的直接标题和说明，保留常规、内部小标题及内嵌插件。以正式宿主CSS进行三页×深浅主题×三视口验证，标题、说明、间距、顶部留白和小标题隔离通过；构建、类型检查通过。已安装codex-ui-official-headings-20260909并核对client.js哈希，清单备份同目录；回退可重装codex-ui-settings-heading-20260909。未提交、推送或发布，正式宿主观感待重载复验。
