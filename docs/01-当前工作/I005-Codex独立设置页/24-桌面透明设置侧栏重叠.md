# 桌面透明设置侧栏重叠

更新时间：2026-09-09 18:56（Asia/Shanghai）

原因：Mica 设置背景透明，被隔离侧栏只有祖先声明 `visibility:hidden`；会话展开容器显式声明 `visibility:visible`，后代重新显示。

修复：仅 Mica 模式且设置页存在时，对原有 inert 分支及全部后代设置 `visibility:hidden!important`，不卸载会话、不改变布局。退出后原选择器失效。

验证：新增 `scripts/verify-settings-backdrop.mjs`，Chromium 先复现 visible/hidden 断言失败，修复后确认侧栏隐藏、设置可见及退出恢复。已纳入默认布局检查；本轮未重复全量测试。

初次验证只替换已安装 UI 1.0.0 的这一条 CSS。随后按用户要求完整构建当前 UI 1.1.0 源码，覆盖安装到 `C:\Users\YUJIYU\.dsh\profiles\web\node_modules\@michengai\dsh-codex-ui`；同时包含关于页宠物入口和欢迎页 LOGO / 标题放大。全部安装文件哈希一致，需要完全退出并重新打开 Desktop，实机视觉复核待用户测试。

类型检查、关于页与依赖管理契约、新建页深浅主题五视口布局、透明设置隔离回归以及构建通过。pnpm 自动同步依赖遇到专家插件链接占用（EBUSY），类型检查改用本地 TypeScript 直接执行。

覆盖前备份位于 `C:\Users\YUJIYU\.dsh\local-backups\codex-ui-local-test-20260909-182409`，可将备份文件覆盖回安装目录后重启恢复。用户随后要求清理备份，但批量删除被工具策略拒绝，备份仍保留。

关联提交：`5733d8c`。源码、测试和费用页截图随该提交保留；当前提交、发布与安装状态统一见[当前状态](../../00-交接入口/02-当前状态.md)。

## 评审增量证据

- 复现：在 body 下添加高 z-index 且后代显式 visible 的 inert portal，原 CSS 的浏览器不可见断言失败。
- 修复：设置隔离分支增加 `data-dcu-settings-isolated` 标记，CSS 在普通主题和 Mica 下隐藏该分支与后代，退出恢复原 inert 和标记。保留引导模态，不扩展为隐藏所有 body portal；打开设置后产生的交互浮层不在该次隔离快照内。
- 验证：设置页 16 用例覆盖引导与菜单隔离、退出恢复；浏览器验证 body portal 后代隐藏。新建页从宿主发布包读取真实 SVG 路径与 viewBox，深浅主题五视口断言 LOGO 宽 46px、比例不变、标题 34px；已查看真实 SVG 的测试截图。仍不是正式 Host 实机验收。
- npm 核验：2026-09-09 官方 registry 的 pet latest 为 0.1.2，tarball 下载成功（12,554,288 字节），SHA-512 与元数据一致；未执行关于页从 npm 安装的端到端流程。
- 初次评审阶段仅跑相关检查。后续 UI 1.1.1 与九成员 Installer 发布准备均重新运行完整 pnpm test，31 文件 / 164 用例、33 契约脚本及全部布局通过；阶段结果不可混用。
- 集合包扩展证据：`c5087fb` 将 pet 追加到第九个成员，锁定 npm 0.1.2；成员契约由失败转为通过，dry-run 确认安装命令包含该包。旧兼容聚合包保持历史依赖范围。

## 发布验收证据

- UI 1.1.1：提交 `37b224c`，官方 npm gitHead 一致；Node.js 22/24 CI、npm 发布、GitHub Release 流程通过，双语正文与 CHANGELOG 一致。
- 九成员 Installer 1.0.3：提交 `c5087fb`，官方 npm gitHead 一致；远端完整测试与发布通过。下载 npm tarball，SHA-512 校验通过，包内清单有九项，pet 0.1.2 / UI 1.1.1 锁定正确，实际 installer.mjs 包含 pet。双语 Release 与 CHANGELOG 一致。
- 当前本机安装及尚未验收的真实安装/宿主流程以交接入口为准，发布不自动关闭实机待办。
