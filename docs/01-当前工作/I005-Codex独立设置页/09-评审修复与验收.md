# 评审修复与验收

更新时间：2026-09-08 22:10（Asia/Shanghai）

## 修复范围

- 全局搜索的设置根条目复用导航入口；新设置页不再被尚未卸载的搜索 dialog 阻止打开，保留旧宿主 dialog 兼容选择器。
- 设置页排除首次使用引导的内联节点与活动 portal，保留原 inert 状态；补齐 Tab 循环与迟挂载背景焦点保护。Escape 只服从可见且非 inert 的对话框。
- 配置文件入口在元数据获取失败且没有 hasDocument 时仍显示错误，异步响应在卸载后不更新状态。补齐真实 Cordis/SlotRegistry 注入生命周期、loopback 开关、服务移除，以及打开成功、失败和重试测试。
- 注册前及 sidebar.settings 注入时检测已有设置壳，发现占用则告警并保留宿主。独立壳接管仍依赖 bundle patch 停用 ui-settings-general；不保证其他插件随后强行重复声明的加载组合。
- 新页图标由 sectionIcon 单独负责；旧 observer 仅保留旧 dialog 兼容用途。插件市场 Store 与侧边卡片 PanelRight 已有行为测试。
- 搜索隐藏当前分类时保留正文并提示选择匹配分类，避免自动切页卸载正在填写的表单。消费 settings.header / settings.close 插槽。
- 双语变更日志将 0.2.114 标为待发布。安装器保持已发布的 0.1.31 快照（UI 0.2.113），断言改为核对安装器自身双语发布快照，不强制与工作区待发布 UI 同版。

## 验证结果

- 类型检查、构建通过；26 个 Vitest 文件、142 个用例通过。
- 33 个断言脚本、57 组 Chromium 布局检查通过（45 工作区、12 侧栏）。
- 生产组件 Chromium 交互验证通过：Shift+Tab 圈闭、过滤提示、隐藏 dialog 不阻 Escape、退出焦点恢复。
- 本机 DSH 1362×901 深色窗口：搜索“设置”并点击根条目，成功进入独立常规页；截图确认插件市场与侧边卡片图标不同，打开配置文件入口存在。未点击业务配置操作。
- 历史 129 / 131 用例分别属于先前验证时点；最新结果以本记录为准，不覆盖历史证据。

## 本机安装与恢复

- 使用 Desktop 自带 Node.js / pnpm 11.24.0 和原 store，以 --offline --ignore-scripts 安装至 C:\Users\YUJIYU\.dsh\profiles\web。
- 修复包与备份：C:\Users\YUJIYU\.dsh\local-packages\codex-ui-review-0.2.114；包含 michengai-dsh-codex-ui-0.2.114.tgz、package.before.json、pnpm-lock.before.yaml。
- 安装 client.js 与工作区构建 SHA-256 一致：1B5A0A5D6A5E62F87D32BD79F364F9C7F6952F67658E45FB5B8BED872299EF3F。
- 恢复时使用同一 Desktop pnpm/store 重装上一个本地包 C:\Users\YUJIYU\.dsh\local-packages\codex-ui-icons-0.2.114\michengai-dsh-codex-ui-0.2.114.tgz；不要仅覆盖清单和锁文件。未修改业务设置或会话数据。

## 遗留与提交

- I005 保持进行中：浅色主题、真实配置保存、全部内层弹窗、首次使用引导全流程及连接重试仍需宿主实机验收，自动化通过不代替这些路径。
- preview-settings.mjs 经 tsx 解析 esbuild 的低优先依赖耦合暂留；本轮未新增依赖。
- 本记录随本地提交 fix: address standalone settings review regressions 纳入版本控制；未推送、未打标签、未发布。
- 前阶段提交：f1bc3ac。任务与待办见[迭代总览](00-迭代总览.md)和[项目待办](../../00-交接入口/03-待办与阻塞.md)。
