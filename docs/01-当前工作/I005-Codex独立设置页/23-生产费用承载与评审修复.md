# 生产费用承载与评审修复

更新时间：2026-09-09 11:32（Asia/Shanghai）

状态：进行中；本轮纳入本地提交 `fix: harden usage frame lifecycle and keyboard navigation`，父提交为 `edb409f`；未推送或发布，实机验收仍待完成。

## 实现

- 费用插件脚本在同源父页完成桥接后加载，脚本 onerror 和资源捕获监听直接报告失败；运行错误保留至握手，React 错误边界覆盖渲染异常。承载 runtime 自身加载失败由父页握手校验识别。
- 仪表盘节点消失只报告面板关闭，设置保留并提供重新打开入口；仅显式无内层弹层 Escape 退出设置。原生 Modal 适配层协调 Escape 优先级和焦点恢复，菜单 Escape 不连带关闭 Modal。
- iframe 首尾 Tab 返回父页相邻控件；失败/面板关闭时将焦点交还父页操作按钮。
- 补译仅在设置项和菜单观察字符变化；全局仅发现控件挂载，流式正文字符变化不触发扫描。
- 默认 640px 是用户要求，有意替换宿主无偏好自适应默认；640/176 常量与宿主发布源码建立漂移断言。
- `usage-frame-routes.ts` 的 JS/CSS 位置以构建后 lib 为基准，开发调试须先构建。

## 验证

- 新增回归先失败后通过：正文流式更新零补译扫描；面板关闭不退出设置。
- `npm test`：31 文件 / 164 用例，33 契约脚本，全部布局检查通过。
- `scripts/verify-usage-runtime.mjs` 加入 `test:layout`：生产构建物、原生 Modal、测试费用组件验证握手后加载、404即时反馈、内层弹窗/菜单Escape、显式退出、面板关闭、Tab返回。CI 无需用户 profile 或第三方包。
- 最后焦点收尾后相关 10 用例、生产 runtime 检查和 build 通过。
- 临时费用预览及配套检查脚本已删除；生产协议检查使用 `scripts/verify-usage-runtime.mjs`，运行前需构建。

## 发布前待验收

真实第三方预算/价格保存、各分区、Desktop Mica组合、首次发送、拖拽手柄位置和Git菜单。自动化不代表这些项目已通过。

## 本地安装与恢复

本轮包：`C:/Users/YUJIYU/.dsh/local-packages/codex-ui-review-fixes-20260909/michengai-dsh-codex-ui-1.0.0.tgz`。
安装前 web profile 的 package.json / pnpm-lock.yaml 备份到 `C:/Users/YUJIYU/.dsh/backups/review-fixes-20260909`。需要恢复时使用备份中原本地包路径重新安装，并重载 DSH；不涉及账单或会话数据迁移。

## 失败状态优先级补丁

基于 `a748dfc` 的后续修复，尚未提交。iframe 出错后不再上报 ready/dismissed；父页失败态不接受这两类迟到事件覆盖，仅重试创建新 iframe 后恢复加载。

父页生命周期测试与生产 runtime 浏览器检查均先失败后通过，覆盖 ready 后渲染崩溃、失败后迟到事件和重试恢复；构建通过。

本地补丁包：`C:/Users/YUJIYU/.dsh/local-packages/codex-ui-usage-failure-20260909/michengai-dsh-codex-ui-1.0.0.tgz`；安装前 profile 备份：`C:/Users/YUJIYU/.dsh/backups/usage-failure-20260909`。未推送或发布。
