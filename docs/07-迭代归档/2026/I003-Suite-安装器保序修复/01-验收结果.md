# I003 验收结果

- 验收状态：通过
- 验收时间：2026-09-04 11:22（Asia/Shanghai）
- 验收范围：Suite 安装器 bundle 归一化、回归测试、构建与文档一致性

## 行为验收

- 第三方 bundle、`base` 和 `web` 保持原有相对加载位置。
- 旧 Suite 在有效锚点原位展开为六个直接成员；锚点早于 `web` 时只移动到最早安全位置。
- 没有旧 Suite 锚点时，六成员追加到末尾，不移动现有 bundle。
- 缺少或颠倒 `base/web` 时抛出明确错误，不静默注入或重排。
- 安装成员和清理旧直接依赖后，写回的是所有增删操作前已验证的顺序快照。

## 自动化证据

测试先行阶段执行 `node tests\codex-suite-installer.assert.mjs`，原实现因强制把 `web` 移到第二位而失败。完成实现后：

- 安装器定向断言：通过。
- `tsc --noEmit`：通过。
- Vitest：18 个测试文件、78 项测试全部通过。
- `tsdown`：Host ESM 与 Client CJS 双入口构建通过。
- 源码契约断言：31 个脚本全部通过。

## 遗留风险

- 安装器仍属于旧版迁移工具；本次不改变其发布与弃用策略。
- 尚未执行真实 profile 安装，避免在未获额外授权时改写本机 DSH profile；纯函数和完整仓库门禁已覆盖本次代码路径。

## 回滚说明

恢复 `packages\dsh-codex-suite-installer\installer.mjs` 的归一化实现并移除对应新增测试即可；本次不涉及用户数据迁移。
