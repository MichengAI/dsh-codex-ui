# I002 审计问题修复

- 状态：已完成
- 负责人：Codex（实施）、项目维护者（发布）
- 优先级：高
- 更新时间：2026-09-04 10:28（Asia/Shanghai）

## 目标与业务边界

修复审计中已复核成立的 DOM 观测性能、依赖路由回归测试、旧安装器 YAML 处理、语言兜底和运行时健壮性问题。保持现有公开接口、工作区数据格式、bundle 加载顺序及 Cordis 强制注入契约不变。

不处理已确认不成立的 `connection` 缺失、`ChannelBrowser.items` 空值、频道与定时树未读状态、Desktop/Web 重复 IPC 及 `turn-navigator` 恒真断言。

## 任务清单

- [x] 通过失败测试锁定流式更新不得触发全文档装饰扫描。
- [x] 补齐 dependencies 路由跨站拒绝与错误脱敏行为测试。
- [x] 为旧安装器不同 YAML 缩进的去重和 Windows 参数字符校验编写边界测试。
- [x] 为语言空串兜底、循环引用等健壮性问题编写边界测试。
- [x] 为 About 后台刷新失败和连接器市场探测补充行为测试。
- [x] 实现最小修复并通过新增测试。
- [x] 执行完整测试、文档校验并完成归档。

## 阻塞事项

无。

## 验收标准

- 流式回答子树变化不会让用户气泡与顶栏装饰器扫描整个 `document`。
- dependencies POST 跨站请求返回 403，路由异常不会向客户端泄露本地路径。
- 安装器可处理两空格、四空格及重复的 `allowBuilds` 条目，并拒绝 Windows `cmd` 展开字符。
- 权限语言兜底跳过空字符串，远程错误循环引用不会递归溢出。
- About 已有数据刷新失败时保留数据并显示本地化非阻断提示；市场探测不依赖 HEAD。
- `pnpm test` 全部通过，文档相对链接有效。

## 已完成内容

- 将流式会话变化收敛为受影响消息根的局部处理，仅在顶栏变化时执行全局顶栏同步。
- 修复权限语言空串兜底、远程错误循环引用、工作区偏好版本常量和 About 后台刷新失败提示。
- 将连接器市场探测由 `HEAD` 改为 `GET`，并在读取前取消响应正文。
- 修复安装器在不同 YAML 缩进下的重复键问题，并拒绝 Windows `cmd` 展开字符。
- 补齐 dependencies 路由跨站拒绝和错误脱敏行为测试，以及上述客户端边界测试。
- 完整执行 `pnpm test`：TypeScript、78 项 Vitest、双入口构建和 31 个断言脚本全部通过。

## 待处理事项

无。版本提交、推送和发布由项目维护者另行授权。

## 关联提交

`d94dfca fix: address codex ui audit findings`

## 阅读顺序

1. 本文档。
2. [验收结果](01-验收结果.md)。
