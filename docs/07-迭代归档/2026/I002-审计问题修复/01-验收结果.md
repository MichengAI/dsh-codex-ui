# I002 验收结果

- 验收状态：通过
- 验收时间：2026-09-04 10:28（Asia/Shanghai）
- 验收范围：审计问题修复、回归测试、构建与文档一致性

## 行为验收

- 流式回答只处理发生变化的消息根，顶栏未变化时不再扫描整份 `document`。
- 权限标签语言选择会跳过空字符串，并继续按宿主语言和浏览器语言兜底。
- About 已有依赖数据刷新失败时保留旧数据，并显示本地化的非阻断提示。
- 连接器市场使用 `GET` 探测，及时取消响应正文；组件卸载后不会更新状态。
- 远程错误链存在循环引用时安全终止，不发生递归栈溢出。
- 工作区偏好响应版本来自统一常量，不再出现协议版本漂移。

## 安全验收

- dependencies 安装路由的跨站 `POST` 返回 403。
- dependencies 路由异常响应经过脱敏，不泄露本地绝对路径。
- Windows `cmd` 参数拒绝 `%`、`^`、`!`、双引号及换行符。
- 安装器在不同 YAML 缩进下去除受管依赖重复键，并保留既有缩进风格。

## 自动化证据

执行 `pnpm test`，结果如下：

- `tsc --noEmit`：通过。
- Vitest：18 个测试文件、78 项测试全部通过。
- `tsdown`：Host ESM 与 Client CJS 双入口构建通过。
- 源码契约断言：31 个脚本全部通过。
- `git diff --check`：通过。

## 遗留风险

- 尚有部分历史 `*.assert.ts` 依赖源码字符串匹配；本迭代已为高风险行为补充真实行为测试，后续仍应按功能变更逐步迁移。
- 当前改动尚未提交、推送或发布，发布前需由维护者复核版本号和双语更新日志。

## 回滚说明

本迭代未修改持久化数据格式或公开接口。若需回滚，恢复本迭代涉及的生产文件并移除对应新增测试即可；工作区偏好文件和会话数据无需迁移。
