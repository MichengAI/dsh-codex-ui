# I006 DSH 0.1.5 双版本兼容

状态：已完成
负责人：Codex
优先级：P1
更新时间：2026-09-11 09:58（Asia/Shanghai）
关联提交：`e94ef9d`、`28ef4ff`、`fb5e686`；本轮 Web 测试脚本与验收文档纳入本次提交 `test: stabilize and complete dual-version web acceptance`

## 当前 Web 验收基线

基于 `fb5e686`，33 文件 / 169 用例、32 断言、类型检查、构建及全部布局检查通过。rc.1 / rc.2 完整 E2E 和设置退出四条路径均通过，缩略图间距严格 6px，pageErrors 为 0；@/指令菜单新增双版本、深浅主题、三个视口共 24 组真实几何检查。

此前 rc.1 复验阻塞为隔离空工作区的候选加载测试竞态，已修复测试准备并关闭。完整事实、产物哈希和范围见 [05 验收记录的当前基线](05-评审优化与最终验收.md)。本轮排除 Desktop，不包含真实模型调用、英文 locale 与第三方全插件组合。

## 28ef4ff 提交时验收基线（历史）

本轮评审优化与 Session 日志清理纳入第二个本地提交。最终构建已在隔离 rc.1 / rc.2 完成扩展真实宿主验收，两版 pageErrors 为 0；日常 Profile 当前只有 IM，无 Codex UI。

- 源码检查：类型检查、构建通过；最终 33 个测试文件、169 个用例通过（maxWorkers=2）。
- 契约与布局：32 断言脚本及全部布局脚本通过，导航使用有版本来源的官方 CSS。
- 宿主范围：真实附件间距（两版均 6px）、@/指令列表、长消息、导航预览与动画、ZIP 下载、全局面板返回和设置退出。
- 证据与限制：[评审优化与最终验收](05-评审优化与最终验收.md)。

## 目标与边界

适配 DSH `0.1.5-rc.1` 和 `0.1.5-rc.2`，完成回归与真实宿主端到端检查。保留旧版附件与导航兼容，不升级插件发行版本。后续按用户授权安装日常 profile 并纳入本次本地提交；不推送、不发布。官方文案补译已按用户要求移除，旧版允许显示英文。

## 已完成

- 统一读取旧版 `imageIds` 与新版 `attachmentIds`，修复空草稿预填、命令历史及方向键召回中的异常，并保护已有图片、普通文件。
- 补齐 `sidebar.panellist` 声明与渲染，复用宿主面板图标及标签，支持语言变化、动态注册/移除、排序、展开/紧凑状态、选中反馈及返回原会话。
- 打开已有会话、派生会话和会话深链接时显式退出全局面板；旧宿主没有 `selectPanel` 时继续正常打开会话。
- 补充新版 renderer 类型声明入口，避免标准属性的品牌类型与旧版兼容定义发生交叉冲突。
- 11 个同步发布的 DSH 客户端 peer 范围明确接受两个候选版本；不同步发布的 legacy runtime 保留原范围。开发依赖仍为旧版回归基线。

## 原始兼容阶段验收（历史，不代表当前构建）

| 检查 | 结果 |
| --- | --- |
| `pnpm test` | 33 文件、170 用例、33 断言脚本通过；类型检查和构建通过 |
| 浏览器布局回归 | 工作区 45 组、侧栏过渡 12 组，输入区/新建页/菜单/调宽/生产费用 iframe/设置背景检查全部通过 |
| rc.1 源码类型检查 | 完整 rc.1 声明环境通过，检查 `src`，不混用旧版 DSH 声明 |
| rc.2 源码类型检查 | 完整 rc.2 声明环境通过，检查 `src` |
| rc.1 真实宿主 E2E | 打包安装、真实工作区与会话、建议预填、历史召回、图片/文件上传与保护、全局面板和设置交互通过，pageerror 为 0 |
| rc.2 真实宿主 E2E | 同上，pageerror 为 0 |
| 两版设置页退出 | 返回、Escape、进入中退出、减少动态效果全部通过 |

附件预填测试先复现旧实现的 TypeError，再修复通过。首次 rc.1 包安装发现 npm 将宽松传递依赖解析到 rc.2，因此隔离运行时按官方同步包精确固定版本；测试入口额外验证 CLI、base、web-app 和关键客户端包版本。历史召回测试用真实键盘全选、删除，并等待编辑器与宿主草稿状态同步，避免将编辑器异步提交当成宿主缺陷。

## 证据与恢复

- 最终 rc.1 隔离 profile：`C:\Users\YUJIYU\AppData\Local\Temp\dcu-compat-host-kSwGlj`。
- 最终 rc.2 隔离 profile：`C:\Users\YUJIYU\AppData\Local\Temp\dcu-compat-host-ht8Wes`。
- 精确运行时：Temp 下 `dcu-runtime-rc1-pinned-20260911` 与 `dcu-runtime-rc2-pinned-20260911`；保留用于重跑验收。
- 脚本自动终止它启动的宿主；测试 fixture 不在发布文件清单中。该历史阶段的日常 Profile 仅含官方组件；后续安装状态以交接入口为准。
- 收尾递归删除早期临时环境被自动审批策略拒绝（blocked by policy），早期试验目录仍保留；测试进程均已停止。
- 真实页面截图：[rc.2](02-rc2真实宿主.png)、[rc.1](03-rc1真实宿主.png)。
- 本次实现未发布，现有 npm 1.1.1 不含这些改动。代码恢复可通过审阅本轮差异逐项撤销；没有真实用户数据迁移。

## 待处理与验证限制

本迭代无阻塞。E2E 的工作区和会话通过真实宿主服务准备，面板由独立 fixture 注册，历史通过浏览器存储预置；点击、键盘、附件上传和设置退出通过真实 DOM 操作。未调用付费模型，未覆盖模型回答、第三方全插件组合及 Desktop 原生材质；这些仍由 I005 的既有实机待办承接，不据此宣称整个插件生态全面兼容。

阅读顺序：[复现测试](01-复现测试.md) → [当前状态](../../../00-交接入口/02-当前状态.md)。

## 用户后续授权的本机测试

2026-09-11 01:10（Asia/Shanghai）：按用户“卸载其他插件，只安装本插件测试”的要求，核实日常 web profile 已无其他第三方插件，安装本次源码重新构建的 tarball。SHA256 与构建一致；全局 DSH 0.1.5-rc.2 启动成功，设置返回、Escape、进入中退出及减少动态效果四项真实宿主检查通过。保留该宿主运行供用户手测；未发布 npm，包版本仍为 1.1.1。此项是隔离验收完成后的新增授权，不属于前文的隔离测试环境。

## 后续样式修复

附件容器、附件间距与新版右栏按钮已修复并重新安装，171 用例及全部布局回归通过；未启动预览。范围、验证边界与安装记录见[新版样式覆盖修复](04-新版样式覆盖修复.md)。

## e94ef9d 提交时验收（历史）

累计适配与后续样式修复统一提交。最终 pnpm test 通过：33 文件、168 用例、32 断言脚本及全部布局回归；测试数量变化来自删除已撤销补译的测试。此前双版本真实宿主验收范围见上文，后续样式使用浏览器夹具验证。该阶段构建曾安装且 SHA256 一致；之后用户已卸载 Codex UI，当前安装状态见交接入口。
