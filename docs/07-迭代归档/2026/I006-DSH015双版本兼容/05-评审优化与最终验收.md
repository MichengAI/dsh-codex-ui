# 评审优化与最终验收

更新时间：2026-09-11 09:36（Asia/Shanghai）

状态：已完成；主兼容提交为 `e94ef9d`，后续清理与评审优化纳入第二个本地提交 `28ef4ff`（fix: finalize native UI cleanup and compatibility regressions）。

## 处理结果

- 面板快照以 id、label、order 逐项比较，保留相同快照引用，移除 JSON 序列化；面板数量少，暂不引入订阅脏标记状态机。
- 返回会话改为 MessageSquare 图标；面板切换通过 selectGlobalPanel 统一能力检测并保留 this，旧宿主缺少方法时不操作。
- 旧导航部件属性仅用于清除旧版注入或热更新残留，注释说明用途，不再新增标记。
- 隔离安装路径比较双方 realpath，再用 path.relative 排除父目录和其他盘符，避免原始短路径和字符串前缀误判。
- Session 日志旧入口清理已纳入 `28ef4ff`；dsh-demo.txt 已不存在，不需要额外删除。
- Peer 范围继续精确接受 rc.1 / rc.2；后续 RC 需重新验收，不以省去版本更新为由自动扩张兼容承诺。

## 测试来源与范围

导航布局夹具使用官方 rc.2 的完整原始 TurnNavigator.module.css，来源 tag、commit、文件路径及 SHA256 记录于 scripts/fixtures/upstream-rc2/source.json。最小 DOM 仍是测试夹具，不宣称从真实会话抓取；真实宿主验收另外运行。

扩展 test:compat：通过隔离宿主的 session/created 事件为 visual 工作区准备三轮静态长消息；数据经官方 Session.append 写入，由真实组件渲染，不改写页面 DOM、不调用模型。真实键盘输入 / 与 @，验证建议内部宽度；悬停原生导航验证预览向右和动画；点击官方更多操作并等待浏览器 ZIP 下载事件。并保留附件上传、草稿历史、全局面板和设置退出验证。

首次脚本尝试暴露的是测试准备和定位问题：fill 没有打开建议菜单、空白首页没有日志入口、会话路径需要读取 header.cwd，以及弹窗有两个关闭按钮。脚本现使用真实键盘、有静态轮次的独立会话和带可见文字的关闭按钮；最终双版本完整通过。

## 真实宿主验收

- 构建包：`C:\Users\YUJIYU\AppData\Local\Temp\dcu-slot-fix-20260911085905\michengai-dsh-codex-ui-1.1.1.tgz`。
- rc.1：`C:\Users\YUJIYU\AppData\Local\Temp\dcu-compat-host-PN3kF0`。
- rc.2：`C:\Users\YUJIYU\AppData\Local\Temp\dcu-compat-host-vkJ2Sh`。
- 两版均通过真实建议列表全宽、长消息无展开覆盖、轮次预览向右且保留动画、日志 ZIP 下载、面板切换与返回原会话、附件保护、四条设置退出交互；pageErrors 均为 0。
- 测试启动的宿主已由脚本停止。日常 Profile 未操作，当前仅安装 IM 0.1.43，无 Codex UI。

## 保留边界

- test:compat 仍为显式验收入口，需要精确宿主运行时及 tarball；未加入普通 pnpm test 或 CI，不宣称日常 CI 已覆盖真实宿主。
- 真实视觉测试为桌面视口；窄屏、模型长名称和项目菜单宽度继续由已有布局夹具覆盖，尚非双版本真实宿主全视口验证。
- 静态长消息覆盖官方渲染和导航，不覆盖模型生成、费用或第三方插件组合。旧版缺失翻译时显示英文属于已接受行为。
- 不推送、不发布、不重新安装日常 Codex UI。历史测试数和安装状态见各历史章节，唯一当前基线见迭代总览。

`28ef4ff` 提交时的验收基线：33 文件、169 用例（maxWorkers=2）、32 断言、类型检查、构建和全部布局检查通过。两个真实宿主安装的 client.js 与本地构建 SHA256 均为 `CD27CC4B86DB7BFEA7789AE5DDE6FD0B1F8E975C94FD099B57C6D6B15D516CCA`。

## 附件 slot 层复核（28ef4ff 历史证据）

旧选择器只匹配输入卡片直接子节点，实际附件轨道位于 display:contents 的 conversation.input.attachments 出口内。补齐夹具层级后先复现 -2px，再限定到该出口后代修复为 6px。真实宿主上传附件后补充 6–8px 可见间距检查。

返回会话图标保留 MessageSquare 语义，描边显式设为 1.6，与插件既有紧凑图标规格对齐。兼容和设置退出脚本固定浏览器语言 zh-CN，主兼容脚本同时断言宿主 active locale 为中文；这是中文双版本验收，不宣称英文路径已测。

当时按外层轨道测量的复验：rc.1 与 rc.2 真实附件间距均为 6px，扩展 E2E 和四条设置退出路径全部通过，pageErrors 均为空。169 用例、32 断言、类型检查、构建和全部布局回归通过。

## 缩略图间距复核（本次提交：fix: scope attachment rail spacing to slot children）

slot 后代选择器同时命中外层 ComposerAttachments 与内层 AttachmentRail 的 _rail 类。补齐嵌套夹具并改测缩略图底部到正文区域顶部后，旧实现实际为 8px，断言 6px 有效失败；slot 后补直接子级 > 后三个视口通过。真实宿主脚本同样直接测量缩略图，严格断言 6px。

本次 pnpm run test:layout、pnpm run build 和 git diff --check 通过。rc.2 完整隔离 E2E 与设置退出四项通过，缩略图间距为 6px，pageErrors 为空；证据目录为 C:\Users\YUJIYU\AppData\Local\Temp\dcu-compat-host-n6oRtc，构建包位于同级 dcu-thumbnail-gap-20260911。

rc.1 首轮及单独复跑均在建议列表全宽断言（verify-compat-host.mjs:89）失败，未运行到附件新断言；证据目录分别为 dcu-compat-host-VsUrCi、dcu-compat-host-NY3MsD。失败原因待查，不能沿用上一提交的双版本通过结论。本轮未重跑单元测试和 32 断言脚本，未改动日常 Profile；本次修复按用户要求提交，不推送或发布。
