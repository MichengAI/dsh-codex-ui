# 新版样式覆盖修复

状态：已完成；关联提交：本次本地提交（fix: adapt to DSH 0.1.5 and refine native UI integration）。
更新时间：2026-09-11 02:55（Asia/Shanghai）

## 修复范围

- 文字气泡通过官方气泡类定位，不再猜测父节点的最后一个子节点。保留纯附件、多附件和引用摘要的布局，撤销旧版对非正文节点留下的错误标记；容器溢出覆盖也仅作用于含有正文的行。
- 输入区继续使用 Codex 的 4px 行间距，将附件轨道的底部补偿调为 2px，保留官方附件到正文的 6px 间距。无附件时输入区高度不变。
- 全局紧凑顶栏把新版 `data-conversation-header-corner` 排在工具之后，清除原嵌套布局的负外边距；没有页签时保留官方顶栏布局。

## 验证

先新增回归用例，确认旧实现误标记附件与引用、附件到正文间距为 -2px，再实施修复。

`pnpm test` 全部通过：34 个测试文件、171 个用例、33 个断言脚本、类型检查、构建及全部布局脚本。新增 `verify-upstream-style-regressions.mjs` 已纳入 `test:layout`，在 390/900/1440px 验证附件容器仍为 flex、仅正文接收展开样式、附件间距为 6px、右栏入口顺序和外边距正确。

新增排版检查使用 0.1.5 官方 DOM/CSS 合约夹具及生产覆盖代码，不等同于真实模型会话验收；前文双版本 E2E 是本次样式修复之前的记录，本轮未重启真实宿主。

## 本机安装

已更新日常 web profile 中唯一的第三方插件 Codex UI，客户端 SHA256 与当前构建一致。第一次使用同名 tarball 更新时发现包管理器仍保留旧产物，改用独立包路径后安装并核验成功：Temp 下 `dcu-style-fix-20260911\michengai-dsh-codex-ui-1.1.1.tgz`。

未启动预览或重启用户的 DSH。用户自行重启宿主、刷新页面后生效；包版本仍为未发布修改的 1.1.1，未提交或发布 npm。

## @ 与指令建议菜单宽度

输入区工具菜单观察器曾把 `[data-trigger-menu]` 内部的 `role=listbox` 当作独立弹窗，赋予 320px 宽度，导致外框全宽而内容只占左侧。现明确排除官方建议菜单及其内部节点，保留宿主宽度、面包屑和滚动布局；普通工具弹窗继续适配。该结构旧版本已存在，并非 rc.2 新引入。

回归用例先复现错误标记，再验证修复。浏览器检查覆盖点击指令按钮、直接键入 @ 两条路径及 390/1280px 视口，断言外框与输入区等宽、内部列表仅扣除官方左右各 4px 内边距，并验证原选项回调。

本轮完整 `pnpm test` 通过：35 文件、172 用例、33 断言脚本及全部浏览器布局检查。修复包已安装到日常 web profile，客户端哈希与本次构建一致；未启动预览、未提交或发布。

## 当前方案：恢复 Codex 外观并自适应宽度

按用户后续要求，@ 与 / 建议菜单重新采用 18px 圆角、白色/深灰背景、13px 紧凑条目及选中底色。样式直接作用于 `[data-trigger-menu]` 外框及其列表；保留前述观察器排除规则，避免内部列表再次被当作独立弹窗。外框左右对齐输入卡片，列表填满扣除左右各 5px 内边距后的空间；动态高度、键盘导航、路径面包屑继续交给 DSH。此方案取代前文“完全保留官方外观”的临时处理。

浏览器回归覆盖点击指令、直接输入 @、直接输入 /，深浅主题与 390/1280px 视口，并在菜单打开时将输入框改为 280px 验证同步调宽。

当前方案完整 `pnpm test` 通过（172 用例、33 断言及全部布局回归），已更新本机插件并核验构建哈希。未启动预览，由用户自行重启 DSH 后刷新页面。

## 项目、模型和权限菜单原版宽度

移除通用工具弹窗的 262/320px 固定宽度及最小/最大宽度覆盖，保留 Codex 配色、圆角、阴影和定位适配。模型菜单沿用官方 max-content、240px 下限、420px 上限及视口减 32px 限制；项目与权限沿用官方 218px 下限和 360px 上限。Git 弹窗保留独立尺寸，@ 和 / 建议继续随输入框全宽显示。

浏览器回归先确认旧样式将模型最小宽度覆盖为 262px，再验证三类菜单在 390/1280px 视口下短名称保持原版下限、长名称扩展且不超过原版上限。完整 `pnpm test` 通过：35 文件、172 用例、33 断言及全部布局脚本。此项使用原版 CSS 约束夹具，未重启真实宿主，不作为本轮真实宿主 E2E 结论。

已通过独立路径 `dcu-menu-width-20260911021938` 打包安装到日常 web profile，安装后客户端 SHA256 与本地构建一致；用户自行重启后刷新页面，未启动预览、未提交或发布。

## 当前方案：用户气泡恢复原版

按用户要求撤销长文本强制展开，移除插件注入的高度、溢出、换行和行数覆盖，并清除历史展开标记。保留旧版替换卡片清理逻辑，用户消息正文完全采用宿主样式。本节取代前文有关正文继续接收展开覆盖的描述。

本轮完整 pnpm test 通过（172 用例、33 断言及全部浏览器布局回归）。已安装到日常 web profile 并核验客户端 SHA256 一致；未启动或重启宿主，由用户自行重启并刷新对比原版效果。

## 移除宿主文案补译

官方 rc.2 已为对话显示、权限名称及六条内置指令说明提供 i18n。按用户最终要求删除 host-copy、permission-labels 两个模块、入口观察器注册、专用词条及已失效的补译测试；不保留旧版兼容分支。插件自身的中英文词典与设置壳不变。旧版缺失官方翻译时显示原始英文，不改变操作功能。

完整 pnpm test 通过：33 文件、168 用例、32 断言脚本及全部布局回归。已更新日常 web profile 并验证客户端 SHA256 一致。未启动或重启宿主，未提交或发布。

## 官方轮次导航仅左移

按用户要求保留导航容器的 left/right 定位，移除刻度按钮和伪元素对齐、预览浮层方向与 animation 覆盖，停止新增内部部件标记；历史标记仍可在清理时移除。官方导航存在时继续隐藏插件自建的旧导航，避免重复。其他组件不变。

完整 pnpm test 通过（168 用例、32 断言及全部布局回归），已更新日常 web profile 并核验客户端 SHA256 一致。本轮未进行真实宿主视觉验收；未启动预览或重启宿主，未提交或发布。

## 左侧导航预览方向修复

前述完全恢复预览定位后，官方向左展开规则使左侧导航的气泡朝侧栏弹出。现仅对已识别官方导航内部的 role=tooltip 设置 right:auto、left:calc(100% + 10px)，让预览朝聊天内容区展开；不重新添加部件标记，不覆盖刻度、气泡外观或动画。

浏览器夹具先复现预览相对导航为 -250px，再验证为右侧 38px（导航宽 28px，加 10px 间距），同时验证官方动画名称与 120ms 时长保持不变。此节取代前文保留官方浮层方向的结论。

完整 pnpm test 通过（168 用例、32 断言及全部布局回归）。本机插件已更新，客户端 SHA256 一致；未启动预览或重启宿主，未提交或发布。本轮视觉验证使用浏览器夹具，真实页面由用户重启后验收。
