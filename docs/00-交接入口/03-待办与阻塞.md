# 待办与阻塞

更新时间：2026-08-27（Asia/Shanghai）

## 发布前

- 审核 `0.2.89` 双语更新日志和最终 diff。
- 确认工作树只包含本次修复后，再由维护者明确授权提交、推送和发布。
- 发布顺序：先发布 `@michengai/dsh-codex-ui@0.2.89`，再发布 `@michengai/dsh-codex-suite-installer@0.1.6`，最后创建/校验 GitHub Release。

## 已知上游约束

- DSH 设置壳没有稳定的“按分区打开”公开 API；兼容跳转集中在 `src/client/settings-navigation.ts`，超时会记录告警。
- Windows Explorer 前台激活依赖 PowerShell、Shell.Application 与 user32；非 Windows 平台回退宿主默认打开方式。
