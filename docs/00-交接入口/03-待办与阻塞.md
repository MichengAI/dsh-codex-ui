# 待办与阻塞

更新时间：2026-09-04 11:22（Asia/Shanghai）

## 发布前

- 确定 Suite 安装器下一版本号，审核最终差异和发布说明。
- 复核 I003 提交后，再由维护者明确授权推送和发布。
- 本次仅需发布 `@michengai/dsh-codex-suite-installer`；根包 `@michengai/dsh-codex-ui` 无生产代码变化，不应联动升版。

## 已知上游约束

- DSH 设置壳没有稳定的“按分区打开”公开 API；兼容跳转集中在 `src/client/settings-navigation.ts`，超时会记录告警。
- Windows Explorer 前台激活依赖 PowerShell、Shell.Application 与 user32；非 Windows 平台回退宿主默认打开方式。
