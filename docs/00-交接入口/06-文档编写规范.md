# 文档编写规范

更新时间：2026-08-27（Asia/Shanghai）

- 文档使用 UTF-8 和简体中文；公开 README/CHANGELOG 保持中英双语。
- `docs/00-交接入口/` 是纳入 Git 的当前事实源：能力写入项目概览，版本事实写入当前状态，未完成项写入待办与阻塞，架构约束写入技术架构基线。
- 历史迭代材料不得覆盖当前事实；若保留在其他 `docs/` 子目录，必须视为本地参考而非发布基线。
- 每次发布准备同步更新日期、版本、安装器钉版、测试命令和发布状态；未实际发布不得写“已发布”。
