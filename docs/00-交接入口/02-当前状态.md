# 当前状态

更新时间：2026-09-11 03:02（Asia/Shanghai）

## 发布与本地阶段

- UI 1.1.1（`37b224c`）已推送并发布；官方 npm latest / gitHead 与双语 Release 正文已核对。范围包含宠物入口、欢迎页 LOGO 46px / 标题 34px、透明设置与外部 portal 隔离修复、费用实验截图及交接文档更新。
- [UI 发布](https://github.com/MichengAI/dsh-codex-ui/actions/runs/34342076152)、[Release](https://github.com/MichengAI/dsh-codex-ui/actions/runs/34342076245) 与 Node.js 22/24 CI 均成功。
- UI 自动发布的 Installer 1.0.2 是八成员快照；用户随后要求集合包加入 pet，`c5087fb` 将 Installer 1.0.3 扩为九成员，锁定 pet 0.1.2 与 UI 1.1.1。[九成员发布流程](https://github.com/MichengAI/dsh-codex-ui/actions/runs/34342596861)与远端 CI 均成功；npm latest、发布 tarball 中的九成员清单和 pet 安装代码、SHA-512、双语 Release 与 CHANGELOG 一致性均已核对。
- 本机全局 DSH 为 0.1.5-rc.2；日常 web Profile 按用户最新要求仅安装当前源码构建的 Codex UI，保留官方 base 与 web-app，无其他第三方插件；客户端 SHA256 与当前构建一致，真实设置页四条退出交互通过。
- pet 官方 npm 0.1.2 tarball 可下载且 SHA-512 一致；日常 profile 已卸载 pet，关于页及集合包的真实 npm 安装流程待验收。旧兼容聚合包保留历史成员范围，九成员扩展位于当前 Suite Installer。

## 本次兼容适配

- 用户消息气泡已取消强制长文本展开，恢复宿主高度、换行和截断规则；旧样式与标记一并清理。

- 新版样式覆盖、@/指令菜单跟随输入框调宽，以及项目/模型/权限菜单恢复原版自适应宽度已安装到日常 profile，哈希一致；172 用例、33 断言及全部布局回归通过。未启动预览，由用户自行重启。见 [样式修复](../07-迭代归档/2026/I006-DSH015双版本兼容/04-新版样式覆盖修复.md)。

- I006 已完成 DSH 0.1.5-rc.1 / rc.2 的附件、全局面板及会话导航适配，源码类型检查、170 用例和完整浏览器回归通过；最终 tarball 双版本真实宿主 E2E 通过。
- 改动纳入本次本地提交、未发布，npm 1.1.1 不含本次修复。验收范围与复现入口见 [I006](../07-迭代归档/2026/I006-DSH015双版本兼容/00-迭代总览.md)。

## 当前功能

- 官方轮次导航移到左侧，预览向右侧内容区展开；刻度、气泡外观和动画使用官方样式。

- 新建页保留宿主 LOGO/文案和底部信息，支持任务建议、独立工具条、两侧拖拽调宽；中间引导不随输入区调宽。
- 默认 640px 是用户明确选择，替代宿主无偏好时的自适应默认；已有有效拖拽记录保留，新建页与会话页共享宽度。
- 使用统计承载原费用组件。关闭原面板留在设置页并可重新打开；显式 Escape 退出设置，内层弹窗和菜单优先处理。跨 iframe Tab 返回父页。
- 设置标题、留白、常规高级区配置入口已接入；宿主文案补译已移除，改用官方翻译。任务侧栏滚动条隐藏但保留滚动，Git 菜单向上定位并消除首帧外观变化。
- 五个配套插件设置标题/按钮布局已本地提交：专家 `d0646e5`、技能 `66e7b4c`、归档 `ec15638`、定时 `5afa5a0`、IM `d751bc1`。IM 保持等宽双栏。

## 验证与边界

- 本轮 UI 1.1.1 与九成员 Installer 改动分别运行完整 `pnpm test` 通过：31 文件 / 164 用例、33 契约脚本、全部布局与生产费用 iframe 检查，含类型检查和构建。此处为本轮重新执行结果，不沿用 1.1.0 的历史数字。
- Installer 成员契约先确认缺少 pet 的失败，再验证九成员清单、精确版本和 bundle 保序通过；dry-run 输出包含 pet 0.1.2 的完整安装命令。真实 SVG 的 46px 宽度、原始比例与标题 34px 已在深浅主题五视口验证。
- 视觉隔离覆盖设置打开时主动隔离的背景分支（含 body portal），退出恢复原始 inert 与标记；不隐藏设置交互期间新建的浮层。欢迎页仍依赖宿主类名后缀，结构变更由契约与尺寸检查暴露。
- I006 已在隔离 rc.1 / rc.2 宿主通过设置页四条退出交互；第三方内层弹窗及 Desktop 原生组合仍待验收。
- 生产 iframe 脚本已纳入 `test:layout`，运行生产构建物及原生 Modal，使用最小测试费用组件模拟公开合约，不读取真实账单。
- 第三方完整预算、价格配置、Desktop 材质组合、首次发送及真实窗口拖拽仍须实机验收；见[待办](03-待办与阻塞.md)。
- 实现、验证和恢复入口见[I005 总览](../01-当前工作/I005-Codex独立设置页/00-迭代总览.md)及[评审修复记录](../01-当前工作/I005-Codex独立设置页/23-生产费用承载与评审修复.md)。
