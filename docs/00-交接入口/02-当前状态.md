# 当前状态

更新时间：2026-08-16 12:45（Asia/Shanghai）

阶段：开发中。

最新变更：本插件已移除设置中的归档会话入口和归档列表页面，归档管理交由独立插件提供；会话三点菜单中的 DSH 归档操作保持不变。置顶会话改为标题后、时间前的低亮度描边图钉；已移除“在新窗口中打开”菜单和实现；工作区树已移除“最近”分区；当前会话所属项目的文件夹图标会高亮，并可通过确认弹窗删除项目注册；项目展开与折叠使用不同文件夹图标。设置窗口尺寸为宽 1000、高 800，并保持距视口 24px 的响应式边距。

活动迭代：I001-Codex-风格界面。

已完成：独立客户端插件骨架；`sidebar` 插槽替换；以更低优先级接管 `sidebar.workspaces` 并重写工作区树；浏览器本地工作区置顶及拖入置顶区域；项目与会话菜单的打开、重命名、归档、派生、ZIP 导出、工作区打开、复制、深链、本地未读和会话置顶；中央会话内容列与输入卡片视觉优化；当前会话轮次导航；统一侧栏与底部设置图标列；移除独立归档入口；修复深浅色主题令牌；专家与技能直达各自可选 peer 插件的设置页，缺失时提示安装；连接器设置分区；归档会话设置分区；全局搜索面板（会话、设置与新建任务）；项目点击折叠且不显示箭头；会话标题与项目标题对齐；项目置顶星标移除；工作区仅以单一蓝点表示本地未读，运行中的会话在右侧显示旋转状态圈；折叠侧栏收紧为 24px，轮次导航在侧栏右侧 16px 并于视口中部垂直居中，默认显示 7px 低对比度微刻度，当前轮次加长为 12px，悬停或键盘聚焦显示 32px 高摘要；扩展二级菜单适度左移；侧栏品牌标识放大；侧栏背景采用 Codex 风格的浅青灰与深灰绿主题色，以及系统无衬线字体栈、12/14px 字号和 Codex 暗色主/次/弱文字层级（`#d1d5d3`、`#9ca39f`、`#747b77`），避免宿主纯白文字过亮；设置窗口调整为宽 1200、高 800，并保持距视口 24px 的响应式边距；补齐 DSH peer/dev 依赖声明、官方 SlotMap 类型合并、中英文词典和 client test runtime 兼容测试；开发依赖升级至公开的 DSH `0.1.0-rc.6`，重建锁文件并接入 jsdom 集成测试；测试纳入 `tsc` 类型检查、补齐 `settings.section` 运行时声明者 `ui-settings-general` 的依赖、`vitest` 运行时路径改为动态解析并消除 sourcemap 告警。

有效风险：连接器目录依赖当前会话；未选择会话时会显示空态或提示先打开会话。专家和技能设置跳转依赖插件公开的显示标签，未安装时会提示对应包名；DSH 当前未公开插件安装 API，因此不能在浏览器确认后安全地自动下载或安装最新包。DSH 目前没有永久删除或撤销归档会话接口；会话置顶和未读是浏览器本地状态。设置壳未公开“按分区打开”API，插件将兼容选择器收敛到单一模块并在失配时记录告警。
