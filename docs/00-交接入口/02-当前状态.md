# 当前状态

更新时间：2026-09-16（Asia/Shanghai）

## 代码与发布

- 已发布版本：Codex UI 1.1.8；Suite Installer 1.0.11（十一成员）。npm 与 GitHub Release 已发布。1.1.8 含 I007 会话规范标题（`emoji 类型｜主题`，不写日期）。
- 发布提交 `6dbbbef`，标签 `v1.1.8`。Installer 见 `f481e4c`。恢复用量入口样式在 1.1.7。

## 未发布增量（`main` 领先 origin）

- 会话标题按 locale 出中英类型词。
- 未安装归档删除能力时隐藏「删除会话」。
- `sessions.current` 换成另一会话时，展开包含该会话的项目或「最近」。
- 工作区树使用官方打开/闭合文件夹字形；运行转圈与项目标题对齐，空聊天 padding 为 `1px 8px 5px 30px`。
- 会话跨项目移动对齐 DSH 0.1.6 SessionHandle（`list` 快照 + `open`/`locate`）。缺磁盘工件映射为 404，不抛裸 ENOENT；双代际目录会改写随目录搬走的旧代际 cwd。
- 隔离宿主 E2E 覆盖 0.1.5-rc.2 与 0.1.6-alpha.1 的侧栏、设置退出和会话移动。标题提供方只核验安装产物，不调模型。

## 本机环境

- 隔离验收使用独立 `DSH_HOME`，覆盖 0.1.5-rc.2 与 0.1.6-alpha.1。日常 DSH 与 web profile 本轮未重启。
- 日常 web 装的是本地未发布构建（含当前会话展开等），不是 npm 1.1.8 原包。归档管理仍按本机既有方式安装。
- 本机安装哈希和 tgz 路径不写入交接文档。

## 当前实现

- 工作区树：项目/分组/最近，当前会话所在文件夹自动展开；无归档删除能力时不出现删除。
- 会话可移动到其他项目；工作目录随目录一起改写。0.1.6 走官方 persist handle。
- 新会话首条用户消息后按 locale 命名一次；宿主钉死后不再覆盖。
- 关于页、设置页、费用承载、附件草稿、全局面板、官方轮次导航与输入建议列表沿用已发布行为。
- Peer 已验收 rc.1 / rc.2；0.1.6-alpha.1 已做隔离宿主回归，尚未写入 peer 范围。

## 验证与边界

- 当前基线：`pnpm test`（类型检查、vitest、断言脚本、布局检查）。隔离 rc.2 / 0.1.6-alpha.1 完整 E2E 与四条设置退出路径已通过。
- 真实宿主测试使用静态会话事件和隔离工作区，不调用付费模型。
- 第三方完整组合、真实模型回答、Desktop 材质与原生窗口拖拽仍未全面验收，见 [待办](03-待办与阻塞.md)。
