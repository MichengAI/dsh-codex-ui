# 当前状态

更新时间：2026-09-16（Asia/Shanghai）

## 代码与发布

- 已发布版本：Codex UI 1.1.9。Suite Installer 仍会由发布流水线在根包成功后自动升一档并钉住 1.1.9。
- 1.1.9 含双语会话标题、无归档删除时隐藏删除、当前会话展开所属文件夹、官方文件夹图标与行对齐，以及 DSH 0.1.6 会话移动（缺工件 404、双代际改写 cwd）。
- 标签 `v1.1.9`。1.1.8 仍是上一档已发布版本，含 I007 基础标题格式。

## 本机环境

- 隔离验收使用独立 `DSH_HOME`，覆盖 0.1.5-rc.2 与 0.1.6-alpha.1。日常 DSH 与 web profile 本轮未重启。
- 日常 web 在发布当时仍可能是本地构建，不是自动换成 npm 1.1.9。本轮未重启 DSH。归档管理仍按本机既有方式安装。
- 本机安装哈希和 tgz 路径不写入交接文档。

## 当前实现

- 工作区树：项目/分组/最近，当前会话所在文件夹自动展开；无归档删除能力时不出现删除。
- 会话可移动到其他项目；工作目录随目录一起改写。0.1.6 走官方 persist handle。
- 新会话首条用户消息后按 locale 命名一次；宿主钉死后不再覆盖。
- 关于页、设置页、费用承载、附件草稿、全局面板、官方轮次导航与输入建议列表沿用已发布行为。
- Peer 已声明 rc.1 / rc.2 / 0.1.6-alpha.1。开发依赖钉在 0.1.6-alpha.1；`dsh-client-runtime` 无 0.1.6 包，仍用已发布最高版 0.1.1-rc.2。
- 未发布改动：独立 `@michengai/dsh-codex-ui/session-title` 插件（inject + `ctx.effect` 回收提供方）、settings locale、主题跟用户消息语言对齐。下一档发布必须升 1.1.10，不得叠在 `v1.1.9` 上推。

## 验证与边界

- 当前基线：`pnpm test`（类型检查、vitest、断言脚本、布局检查）。`test:compat` 第三段曾在 Node 里调用 `CSS.escape` 崩溃，现已改为页面内转义；本轮未重跑隔离完整 E2E。
- `pnpm-workspace.yaml` 的 `packageExtensions` 只补 0.1.6-alpha.1 `@deepseek-ai/dsh-client-ui-primitives` 未声明的 markdown/shiki 依赖，给 vitest 用。正式包补全后应删除。
- 真实宿主测试使用静态会话事件和隔离工作区，不调用付费模型。
- 第三方完整组合、真实模型回答、Desktop 材质与原生窗口拖拽仍未全面验收，见 [待办](03-待办与阻塞.md)。
