# 当前状态

更新时间：2026-09-18（Asia/Shanghai）

## 代码与发布

- 已发布版本：Codex UI 1.1.13。Suite Installer 会由发布流水线在根包成功后自动升一档并钉住当时的 UI 版本。
- 工作区已适配 DSH 0.1.6-alpha.2（双路径会话导航、官方插件管理页副本、SessionStatus、连接器草稿生命周期），随 1.1.13 发布。

## 本机环境

- 隔离验收使用独立 `DSH_HOME`，覆盖 0.1.5-rc.1、0.1.5-rc.2、0.1.6-alpha.1 与 0.1.6-alpha.2。日常 DSH 与 web profile 本轮未重启。
- 本机安装哈希和 tgz 路径不写入交接文档。

## 当前实现

- 工作区树：项目/分组/最近，当前会话所在文件夹自动展开；无归档删除能力时不出现删除。
- 会话可移动到其他项目；工作目录随目录一起改写。0.1.6 走官方 persist handle。
- 新会话首条用户消息后按 locale 命名一次；宿主钉死后不再覆盖。标题提供方由独立 `@michengai/dsh-codex-ui/session-title` 插件注册，卸载时回收。
- 会话行右侧显示紧凑相对时间；悬停或菜单打开时让位给归档快捷图标。截断标题在悬停时滚动全文。
- 运行指示灯和完成后未读优先读 `useSessionStatus`（`running` / `completionUnread`），没有再回退 `SessionSummary.running` 和本地 running 边沿。
- 打开会话失败时保留当前全局面板。全局面板打开时不把主视图会话标成选中。
- 连接器 Prompt：有 `uiWorkspace.openWorkspace` 时只走它（内部已 connect + beforeOpen）。没有再 `connectWorkspace` + `openConversationWithDraft`。不要两路连开，否则会多一个空会话。
- 设置「插件配置」转交官方 `plugins.item` / `bundle.config` / `row.config`，绑定 `hooks` 与 `keyedHooks`，文案转发插值参数。
- 设置页和用量统计挂到 `document.body`，避免收缩窄轨的 transform 包含块困住全屏页。
- 侧栏底部按 slot id 逐条 `renderSlot`；每次调用各自一个 `data-slot` 锚点，无 id 的动作丢弃。`.dcu-footer-actions` 保持竖排，不要改成横向 flex。
- 关于页、费用承载、附件草稿、全局面板、官方轮次导航与输入建议列表沿用已发布行为。
- Peer 已声明 rc.1 / rc.2 / 0.1.6-alpha.1 / 0.1.6-alpha.2。开发依赖钉在 0.1.6-alpha.2；`dsh-client-runtime` 无 0.1.6 包，仍用已发布最高版 0.1.1-rc.2，peer 显式 `|| 0.1.1-rc.2`。
- `pnpm-workspace.yaml` 的 `packageExtensions` 为官方 primitives 补 `simple-icons@16.31.0`（alpha.2 只在其 devDependencies 声明），以及 markdown/shiki 依赖，给 vitest 用。正式包补全后应删除。

## 验证与边界

- 2026-09-18 完整验收已跑：`pnpm test`（42 文件 / 269 用例、35 断言、布局全过）；隔离 `test:compat` 四段全过。证据目录在 Temp：`dcu-compat-host-pqdSkc`（rc.1）、`dcu-compat-host-5K15zz`（rc.2）、`dcu-compat-host-A0dbWR`（alpha.1）、`dcu-compat-host-Y1qoTF`（alpha.2）。受测构建与日常已装 `client.js` 哈希一致。
- 日常 alpha.2 web（未重启、未占 3080）已验收：当前行「标记未读」显示未读点且菜单切到「标记已读」；设置保留常规 / 内置插件 / 插件配置 / 连接器；插件配置不崩壳；连接器 Prompt 只走 `openWorkspace`，草稿写入打开的会话，没有多出一个空会话。
- 兼容脚本的当前会话 id 走生产 `currentSessionId`（`window.__dcuCurrentSessionId`），不再在脚本里重抄弱化版。
- 真实宿主测试使用静态会话事件和隔离工作区，不调用付费模型。
- 第三方完整组合、真实模型回答、Desktop 材质与原生窗口拖拽仍未全面验收，见 [待办](03-待办与阻塞.md)。
- 伴随套件成员 peer 仍钉 alpha.1 时，alpha.2 宿主机上一键安装会报 peer 不满足。Installer 1.0.16 只钉 UI 1.1.13，不改其他成员 peer。
- 日常页仍有宿主噪音 `conversation.chat.turnTail requires options.id`，不是本轮 UI 回归。alpha.1 隔离 CLI 必须把全部 `@deepseek-ai/dsh-*` override 到 alpha.1；只钉 8 个被检查包会混入 alpha.2 的 `dsh-app-boot`。
