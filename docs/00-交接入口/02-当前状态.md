# 当前状态

更新时间：2026-09-09 11:14（Asia/Shanghai）

## 发布与本地阶段

- 已发布基线：Codex UI / Suite Installer 1.0.0，见[发布验收](../01-当前工作/I005-Codex独立设置页/10-1.0.0发布验收.md)。安装器八成员精确版本以该记录为准。
- UI 本轮本地提交：`fix: harden usage frame lifecycle and keyboard navigation`；上一提交 `edb409f`，更早为 `3a16eed`、`324637b`、`e5965af`；这些阶段改动未推送或发布。
- 本轮评审修复已纳入上述本地提交：费用生产 iframe 加载/键盘/关闭链、补译观察范围、宿主宽度契约及交接资料。版本保持 1.0.0，仅供本地测试；下次正式发布必须使用新版本。

## 当前功能

- 新建页保留宿主 LOGO/文案和底部信息，支持任务建议、独立工具条、两侧拖拽调宽；中间引导不随输入区调宽。
- 默认 640px 是用户明确选择，替代宿主无偏好时的自适应默认；已有有效拖拽记录保留，新建页与会话页共享宽度。
- 使用统计承载原费用组件。关闭原面板留在设置页并可重新打开；显式 Escape 退出设置，内层弹窗和菜单优先处理。跨 iframe Tab 返回父页。
- 设置标题、留白、常规高级区配置入口及宿主补译已接入。任务侧栏滚动条隐藏但保留滚动，Git 菜单向上定位并消除首帧外观变化。
- 五个配套插件设置标题/按钮布局已本地提交：专家 `d0646e5`、技能 `66e7b4c`、归档 `ec15638`、定时 `5afa5a0`、IM `d751bc1`。IM 保持等宽双栏。

## 验证与边界

- 全量检查：31 文件 / 164 用例、33 契约脚本及全部布局检查通过；焦点收尾后相关 10 用例、生产 iframe 脚本和构建再次通过。
- 生产 iframe 脚本已纳入 `test:layout`，运行生产构建物及原生 Modal，使用最小测试费用组件模拟公开合约，不读取真实账单。
- 第三方完整预算、价格配置、Desktop 材质组合、首次发送及真实窗口拖拽仍须实机验收；见[待办](03-待办与阻塞.md)。
- 实现、验证和恢复入口见[I005 总览](../01-当前工作/I005-Codex独立设置页/00-迭代总览.md)及[评审修复记录](../01-当前工作/I005-Codex独立设置页/23-生产费用承载与评审修复.md)。
