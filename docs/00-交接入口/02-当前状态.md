# 当前状态

更新时间：2026-09-08 11:27（Asia/Shanghai）

阶段：Codex UI `0.2.111` 与配套安装器 `0.1.29` 已发布；业务 REST 规范化已提交为 `16369d3`，尚未发布。分组无新增缩进优化、统一拖拽指示线和空状态调整随本轮提交纳入版本控制，已安装本地，尚未推送或发布。

## 当前焦点

- 空聊天居中补充随本轮提交：取消 `.dcu-wb-collections` 直接渲染项目时误用的 12px 分组间距。浏览器修复前普通项目上 5px/下 17px，修复后三类项目列表均为 5px/5px；45 组浏览器检查、构建与 32 个断言通过，已更新本地包供用户确认。

- 前轮侧栏改动已提交为 `df0def9`，未推送。本轮空状态补充随 `fix: align sidebar empty states` 提交：有后续分组时“暂无项目”内部上 14px、下 2px，叠加外部 12px 后上下视觉间距均 14px；两种空状态统一 13px/18px/三级灰色。浏览器 42 组组合、构建与 32 个断言通过，已更新本地安装供重启确认，尚未推送或发布。

- 分组空状态上下留白统一为 8px，保留左侧 26px 对齐；“没有聊天”统一改为“暂无聊天”。浏览器确认上下留白一致且收起高度为 0，构建与 32 个断言通过，已更新本地包供重启验收。

- 分组“暂无项目”左侧留白调整为 26px，与标题文字对齐，仅影响分组直属空状态。三种侧栏宽度浏览器测量误差 0px，构建与 32 个断言通过；已更新本地包，待用户重启确认。

- 参考本机 Codex 26.901.6511.0 包内插入线组件，缩减蓝线起点留白，改为连续的 8px 空心圆环和 2px 圆头线，保留右侧 8px 留白与几何居中。113 项测试、32 个断言、构建和 36 组浏览器样式检查通过，已更新本地包供用户重启验收。

- 排序插入线统一使用相邻可见行边界中点，独立绘制层覆盖分组、项目与会话；拖拽期间跟随滚动及折叠更新，不改变胶囊宽度。113 项测试、构建与断言验证通过；真实浏览器样式测试页的深浅主题、三种侧栏宽度、六类落点共 36 组组合及滚动误差均为 0px，实际 Desktop 拖拽仍待用户确认。

- 会话列表上下间距统一为 4px，列表内部仍为 2px；补齐末尾内部留白，防止与下一项目胶囊贴合，收起不残留间距。106 项测试、32 个断言、类型检查与构建通过，更新本地安装后待用户确认真实侧栏。

- 修复分组排序时第一组顶部蓝条被裁剪：仅在展开分区出现分组落点时解除裁剪，不改变缩进及胶囊布局。106 项测试、32 个断言脚本、类型检查与构建通过，已更新本地 Desktop 使用的 web profile，待重启后确认真实拖拽效果。

- 三层胶囊连续显示时，补齐分组标题到首个项目的 4px 垂直留白；与项目到会话的间距一致，不增加横向缩进。

- 分组标题增强为 14px/600、32px 行高和 12px 组间距；项目展开/收起切换文件夹图标，保持原有胶囊位置。预览 DOM 的字重、尺寸、内边距与鼠标/键盘切换图标检查通过。

- 当前版强化分组箭头、标题轻底色与组间留白，撤回新增的项目及会话缩进；102 项 Vitest、32 个断言脚本、类型检查和构建通过。源码预览 DOM 检查确认两版本项目 8px、会话 28px 原有内边距一致，分组展开与收起底色相同。浏览器截图、窄视口和宿主拖拽尚未验收，详见[迭代总览](../01-当前工作/I001-Codex-风格界面/00-迭代总览.md)。玻璃与费用入口组合验收、输入历史的真实宿主交互验收继续由[项目待办](03-待办与阻塞.md)承接。

## 当前事实

- 发布验收：[UI npm 发布](https://github.com/MichengAI/dsh-codex-ui/actions/runs/34170490628)、[Release 说明](https://github.com/MichengAI/dsh-codex-ui/actions/runs/34170490584)、[CI](https://github.com/MichengAI/dsh-codex-ui/actions/runs/34170490557)、[Installer 发布](https://github.com/MichengAI/dsh-codex-ui/actions/runs/34170545380) 全部成功。[UI v0.2.111](https://github.com/MichengAI/dsh-codex-ui/releases/tag/v0.2.111) 和 [Installer v0.1.29](https://github.com/MichengAI/dsh-codex-ui/releases/tag/suite-installer-v0.1.29) 均为正式发布，两份正文与对应中英文 CHANGELOG 一致。

- Codex UI 标题只显示实际运行版本，继续沿用关于页原有升级能力；专家、技能、归档会话、定时任务和 `@michengai/dsh-im-connect` 由统一客户端控制器显示版本并提供检查更新弹窗。归档插件设置入口与页面标题已统一为“归档会话 / Archived sessions”。Codex UI 全量测试、构建及浏览器视觉验收通过；归档插件构建和 20 项相关测试通过，完整验证受本机测试依赖链接缺失影响，未发现本次代码失败。
- Suite Installer `0.1.29` 锁定 UI `0.2.111`、Archive Manager `0.1.32`、Skills Manager `0.1.42`、Agency Agents `0.1.34`、IM Connect `0.1.38`、Automation `0.1.34`、BTW `0.1.4` 和 Simplify `0.1.2`。八成员均取发布时的官方 npm `latest`，安装时不重新解析；要求 Node.js `22.19.0+`。旧聚合包仍保留历史六成员声明权。
- 第三方费用插件 `@kenz1117/dsh-ui-usage-billing` 的侧栏入口已适配为内嵌状态行，保留原生统计和详情功能，随 `0.2.106` 发布；本机 link 已更新。当前 Web profile 未安装壁纸插件，玻璃与费用入口组合验收由[项目待办](03-待办与阻塞.md)承接；已有验收范围与限制见 [I001 迭代总览](../01-当前工作/I001-Codex-风格界面/00-迭代总览.md)。
- 已将上下键输入历史从 BTW 迁入 Codex UI，独立挂载输入扩展，按工作区持久化并导入旧历史。输入栏提供中英文“↑↓ 翻历史”提示，与占位文字同一行靠右，窄栏隐藏，不需要 Alt；参考开源 Codex CLI 补齐召回文本光标首尾与选区保护。迁入提交 `fa54dd2` 和评审修复 `9610307` 已随 `0.2.104` 发布：只消费实时追加事件，补齐 input-trigger peer、自身同步召回的 input 保护与窗口版本归零兼容。全量测试、构建和发布流程通过；本地安装已核对，真实宿主交互未验收。详见 [I004](../07-迭代归档/2026/I004-输入历史迁入/00-迭代总览.md)。
- 关于页已在自研插件末尾加入 BTW 旁问，复用版本检测和安装更新入口；归档插件名称简化为“归档会话”，中英文同步，随 `0.2.104` 发布。BTW 包名、顺序及中英文名称契约检查通过。
- 关于页已在 BTW 下方加入 Simplify（`@michengai/dsh-simplify`），复用版本检测、安装、更新和批量更新入口；插件市场仍保持在其后，随 `0.2.105` 发布。包名、顺序及中英文名称契约检查通过。
- Codex UI 侧栏已兼容 `dsh-plugin-wallpaper-engine` 的玻璃变量，随 `0.2.106` 发布。滤镜仅作用于背景伪元素，避免设置与搜索弹窗被侧栏裁切或主会话遮挡；桌面验证通过，本机 link 构建已更新。390px 窄屏仍有宿主双栏内容拥挤限制，未修改宿主布局。验收结论见 [I001 迭代总览](../01-当前工作/I001-Codex-风格界面/00-迭代总览.md)。

- 展开侧栏按 Codex 对齐：默认 275px、最小 240px、最大 520px；拖到小于 240px 时收缩。设置窗口为 1000×800；会话阅读列为 800px。
- 置顶工作区的事实源是 Host preferences；浏览器本地状态仍用于未读、会话置顶与树展开状态。
- 客户端正式依赖由 DSH 宿主提供；开发/测试依赖对齐公开 DSH `0.1.2-rc.1`，其中当前未发布同版本的 `dsh-client-runtime` 使用 `0.1.1-rc.2`。
- 会话 DOM 观察器按受影响消息根局部处理；五个业务 REST 的新旧路径、401、403、认证不可用、可信代理 Origin 和错误脱敏已有行为测试。
- Suite 安装器只替换受管 bundle，并保留无关 bundle 的既有顺序；缺少或颠倒 `base/web` 基线时会中止迁移。
- Suite 安装器保序修复已作为独立提交纳入生产代码、测试和 I003 归档证据。
