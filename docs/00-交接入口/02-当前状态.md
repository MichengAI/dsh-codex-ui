# 当前状态

更新时间：2026-09-11 12:48（Asia/Shanghai）

## 代码与发布

- 已发布版本：Codex UI 1.1.2；Suite Installer 1.0.4（九成员）。npm 与 GitHub Release 已发布，包含 I006 兼容、样式清理及频道归档修复。
- 发布提交 `1d2c38e`，标签 `v1.1.2`；安装器自动提交 `bf8b881` 已同步本地。详见 [发布验收](../07-迭代归档/2026/I006-DSH015双版本兼容/06-1.1.2发布验收.md)。
- 频道归档成功即时隐藏，旧轮询响应不再使会话重新出现；失败保留会话并显示错误。

## 本机环境

- 全局 DSH：0.1.5-rc.2。
- 日常 web Profile 已安装包含连接器中性灰配色与使用统计 864px 宽度统一的 Codex UI 1.1.1 本地构建，同时存在用户后来安装的其他第三方插件。本轮仅更新 Codex UI，未启动或重启 DSH。
- 上次本地安装的 client.js SHA256：`AFE78B745E6126097C76CB3E2C11D3F4B8A043F43333A965A5457A5352788C1B`；本轮发布未更新日常安装，该哈希不代表新发布的 1.1.2。
- 本轮使用独立临时 Profile 验证 Codex UI，结果归 [I006](../07-迭代归档/2026/I006-DSH015双版本兼容/00-迭代总览.md)。

## 当前实现

- 新旧附件草稿兼容；全局面板支持紧凑侧栏、动态注册、返回已有会话。
- 项目、模型、权限菜单使用原版宽度约束并保留 Codex 外观；@ 与指令列表跟随输入框全宽。
- 用户消息气泡、Session 日志入口和官方文案翻译使用宿主实现；旧版缺失翻译时允许显示英文。
- 官方轮次导航移到左侧，预览向右，保留刻度、气泡外观和动画。
- 新建页与会话页共享宽度偏好；无有效偏好时默认 640px。
- Peer 仅明确接受已验收的 rc.1 / rc.2，不自动承诺后续 RC；旧版 runtime 范围不变。

## 验证与边界

- 当前基线：类型检查、构建、34 文件 / 172 用例、32 断言及全部布局检查通过。隔离 rc.1 / rc.2 完整 E2E 与四条设置退出路径均通过，缩略图间距严格 6px，pageErrors 为 0。

- @/指令菜单覆盖双版本 × 深浅主题 × 390/900/1440px，共 24 组真实几何检查。此前 rc.1 失败源于空工作区候选消失的测试竞态，已补真实候选文件并关闭待办。完整记录见 [I006 验收](../07-迭代归档/2026/I006-DSH015双版本兼容/05-评审优化与最终验收.md)。
- 真实宿主测试使用静态会话事件和隔离工作区，不调用付费模型；原生组件负责渲染，浏览器执行交互。
- 第三方完整组合、真实模型回答、Desktop 材质与原生窗口拖拽仍未全面验收，见 [待办](03-待办与阻塞.md)。
- 独立设置页、费用承载及九成员安装器的设计与历史验收见 [I005](../01-当前工作/I005-Codex独立设置页/00-迭代总览.md)。
