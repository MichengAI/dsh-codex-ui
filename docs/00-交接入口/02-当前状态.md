# 当前状态

更新时间：2026-09-11 09:00（Asia/Shanghai）

## 代码与发布

- 已发布版本：Codex UI 1.1.1；Suite Installer 1.0.3（九成员）。公开 npm 1.1.1 不包含 I006 的兼容适配和后续修复。
- I006 主提交：`e94ef9d`。Session 日志旧入口清理及评审优化纳入本次第二个本地提交（fix: finalize native UI cleanup and compatibility regressions）；没有推送或发布。
- 本次优化：面板快照字段比较、返回会话图标、统一面板 API 检测、隔离安装路径归属校验、旧 DOM 标记清理说明，以及真实宿主视觉回归扩展。

## 本机环境

- 全局 DSH：0.1.5-rc.2。
- 日常 web Profile 当前有 IM 插件 0.1.43，没有 Codex UI 和 better-sidebar；本轮未修改日常 Profile、未启动日常预览或重启用户服务。
- 先前 Codex UI 安装和哈希记录仅代表历史验收，不代表当前安装状态。
- 本轮使用独立临时 Profile 验证 Codex UI，结果归 [I006](../07-迭代归档/2026/I006-DSH015双版本兼容/00-迭代总览.md)。

## 当前实现

- 新旧附件草稿兼容；全局面板支持紧凑侧栏、动态注册、返回已有会话。
- 项目、模型、权限菜单使用原版宽度约束并保留 Codex 外观；@ 与指令列表跟随输入框全宽。
- 用户消息气泡、Session 日志入口和官方文案翻译使用宿主实现；旧版缺失翻译时允许显示英文。
- 官方轮次导航移到左侧，预览向右，保留刻度、气泡外观和动画。
- 新建页与会话页共享宽度偏好；无有效偏好时默认 640px。
- Peer 仅明确接受已验收的 rc.1 / rc.2，不自动承诺后续 RC；旧版 runtime 范围不变。

## 验证与边界

- 最终基线：33 文件、169 用例、32 断言及全部布局检查通过；隔离 rc.1 / rc.2 真实附件间距均为 6px，扩展 E2E 通过。详见 I006 当前验收表。170/171/172 等数字是早期阶段记录，不是当前基线。
- 真实宿主测试使用静态会话事件和隔离工作区，不调用付费模型；原生组件负责渲染，浏览器执行交互。
- 第三方完整组合、真实模型回答、Desktop 材质与原生窗口拖拽仍未全面验收，见 [待办](03-待办与阻塞.md)。
- 独立设置页、费用承载及九成员安装器的设计与历史验收见 [I005](../01-当前工作/I005-Codex独立设置页/00-迭代总览.md)。
