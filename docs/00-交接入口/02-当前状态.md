# 当前状态

更新时间：2026-09-06 16:52（Asia/Shanghai）

阶段：当前源码基线为 `0.2.104`，准备发布；npm 已发布基线仍为 `0.2.103`，配套安装器已发布版本为 `0.1.20`，本次将由工作流自动递增为 `0.1.21`。

## 当前焦点

- 发布 `0.2.104` 根包与 `0.1.21` Suite 安装器，验证 npm、GitHub Release 及双语说明一致性。

## 当前事实

- 已将上下键输入历史从 BTW 迁入 Codex UI，独立挂载输入扩展，按工作区持久化并导入旧历史。输入栏提供中英文“↑↓ 翻历史”提示，与占位文字同一行靠右，窄栏隐藏，不需要 Alt；参考开源 Codex CLI 补齐召回文本光标首尾与选区保护。两插件不再重复绑定键盘；迁入已提交为 `fa54dd2`，尚未发布。后续评审修复已改为只消费实时追加事件，补齐 input-trigger peer、自身同步召回的 input 保护与窗口版本归零兼容；全量测试和构建通过，修复纳入本次提交，尚未发布。验收见 [I004](../07-迭代归档/2026/I004-输入历史迁入/00-迭代总览.md)。
- 关于页已在自研插件末尾加入 BTW 旁问，复用版本检测和安装更新入口；归档插件名称简化为“归档会话”，中英文同步，已纳入 `fa54dd2`。后续补充了 BTW 包名、顺序及中英文名称契约检查。

- 展开侧栏按 Codex 对齐：默认 275px、最小 240px、最大 520px；拖到小于 240px 时收缩。设置窗口为 1000×800；会话阅读列为 800px。
- 置顶工作区的事实源是 Host preferences；浏览器本地状态仍用于未读、会话置顶与树展开状态。
- 客户端正式依赖由 DSH 宿主提供；开发/测试依赖对齐公开 DSH `0.1.2-rc.1`，其中当前未发布同版本的 `dsh-client-runtime` 使用 `0.1.1-rc.2`。
- 会话 DOM 观察器按受影响消息根局部处理；dependencies 路由的跨站拒绝与错误脱敏已有行为测试。
- Suite 安装器只替换受管 bundle，并保留无关 bundle 的既有顺序；缺少或颠倒 `base/web` 基线时会中止迁移。
- Suite 安装器保序修复已作为独立提交纳入生产代码、测试和 I003 归档证据。
