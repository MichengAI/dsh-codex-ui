# 当前状态

更新时间：2026-09-07 01:42（Asia/Shanghai）

阶段：Codex UI `0.2.106` 与配套安装器 `0.1.24` 已发布到 npm 和 GitHub Release；安装器新增 BTW 和 Simplify，共八个成员。UI 包含 Wallpaper Engine 侧栏玻璃兼容与费用入口样式适配，两项已分别验证，组合验收仍待完成。

## 当前焦点

- 维护 `0.2.106` 根包与 `0.1.24` Suite 安装器；玻璃与费用入口组合验收、输入历史的真实宿主交互验收继续由[项目待办](03-待办与阻塞.md)承接。

## 当前事实

- Suite Installer `0.1.24` 已加入 BTW `0.1.3` 和 Simplify `0.1.2`，八成员均锁定发布时的官方 npm `latest`，安装时不重新解析；要求 Node.js `22.19.0+`。旧聚合包仍保留历史六成员声明权。公开双语发布说明仅保留用户可见变化，内部验收进度继续留在迭代和待办中；发布证据见 [I001 迭代总览](../01-当前工作/I001-Codex-风格界面/00-迭代总览.md)。
- 第三方费用插件 `@kenz1117/dsh-ui-usage-billing` 的侧栏入口已适配为内嵌状态行，保留原生统计和详情功能，随 `0.2.106` 发布；本机 link 已更新。当前 Web profile 未安装壁纸插件，玻璃与费用入口组合验收由[项目待办](03-待办与阻塞.md)承接；已有验收范围与限制见 [I001 迭代总览](../01-当前工作/I001-Codex-风格界面/00-迭代总览.md)。
- 已将上下键输入历史从 BTW 迁入 Codex UI，独立挂载输入扩展，按工作区持久化并导入旧历史。输入栏提供中英文“↑↓ 翻历史”提示，与占位文字同一行靠右，窄栏隐藏，不需要 Alt；参考开源 Codex CLI 补齐召回文本光标首尾与选区保护。迁入提交 `fa54dd2` 和评审修复 `9610307` 已随 `0.2.104` 发布：只消费实时追加事件，补齐 input-trigger peer、自身同步召回的 input 保护与窗口版本归零兼容。全量测试、构建和发布流程通过；本地安装已核对，真实宿主交互未验收。详见 [I004](../07-迭代归档/2026/I004-输入历史迁入/00-迭代总览.md)。
- 关于页已在自研插件末尾加入 BTW 旁问，复用版本检测和安装更新入口；归档插件名称简化为“归档会话”，中英文同步，随 `0.2.104` 发布。BTW 包名、顺序及中英文名称契约检查通过。
- 关于页已在 BTW 下方加入 Simplify（`@michengai/dsh-simplify`），复用版本检测、安装、更新和批量更新入口；插件市场仍保持在其后，随 `0.2.105` 发布。包名、顺序及中英文名称契约检查通过。
- Codex UI 侧栏已兼容 `dsh-plugin-wallpaper-engine` 的玻璃变量，随 `0.2.106` 发布。滤镜仅作用于背景伪元素，避免设置与搜索弹窗被侧栏裁切或主会话遮挡；桌面验证通过，本机 link 构建已更新。390px 窄屏仍有宿主双栏内容拥挤限制，未修改宿主布局。验收结论见 [I001 迭代总览](../01-当前工作/I001-Codex-风格界面/00-迭代总览.md)。

- 展开侧栏按 Codex 对齐：默认 275px、最小 240px、最大 520px；拖到小于 240px 时收缩。设置窗口为 1000×800；会话阅读列为 800px。
- 置顶工作区的事实源是 Host preferences；浏览器本地状态仍用于未读、会话置顶与树展开状态。
- 客户端正式依赖由 DSH 宿主提供；开发/测试依赖对齐公开 DSH `0.1.2-rc.1`，其中当前未发布同版本的 `dsh-client-runtime` 使用 `0.1.1-rc.2`。
- 会话 DOM 观察器按受影响消息根局部处理；dependencies 路由的跨站拒绝与错误脱敏已有行为测试。
- Suite 安装器只替换受管 bundle，并保留无关 bundle 的既有顺序；缺少或颠倒 `base/web` 基线时会中止迁移。
- Suite 安装器保序修复已作为独立提交纳入生产代码、测试和 I003 归档证据。
